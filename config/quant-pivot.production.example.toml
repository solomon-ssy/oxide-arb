# quant-pivot — Deploy Configuration (Production Example)
# GENERATED BY `cargo xtask config render`; DO NOT EDIT BY HAND.
# Every process-bound field is derived from the Rust DeployConfig descriptor.
# Loading requires `--config-file <absolute-path>` and `--expected-environment <name>`.
# There is no default path, directory discovery, environment source, overlay, or default fill.
# Copy this file to an untracked 0600 file before adding any real secret.

[deployment]
# Field: deployment.environment
# Purpose: Exact environment identity that must match the mandatory CLI expectation at startup.
# Requirement: Required; recommended value: "production".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-models secure loader and destructive-operation guards; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-models secure loader and destructive-operation guards
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
environment = "production"

[polymarket]
# Field: polymarket.clob_base_url
# Purpose: CLOB REST base URL. Default: `https://clob.polymarket.com`.
# Requirement: Required; recommended value: "https://clob.polymarket.com".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
clob_base_url = "https://clob.polymarket.com"
# Field: polymarket.clob_ws_url
# Purpose: CLOB market-data WebSocket URL. Default: `wss://ws-subscriptions-clob.polymarket.com/ws/market`.
# Requirement: Required; recommended value: "wss://ws-subscriptions-clob.polymarket.com/ws/market".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
clob_ws_url = "wss://ws-subscriptions-clob.polymarket.com/ws/market"
# Field: polymarket.order_post_timeout_ms
# Purpose: Hard timeout for the single `POST /order` attempt, in milliseconds. A timeout is ambiguous and must be reconciled; it is never retried.
# Requirement: Required; recommended value: 45000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.polymarket.order-post-timeout] When Always: The value must be at least 35000 ms so it contains the SDK's 30-second asynchronous commit identity-enrichment budget.
# Operational impact: Changes quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
order_post_timeout_ms = 45000
# Field: polymarket.clob_market_info_refresh_secs
# Purpose: Refresh cadence for append-only CLOB market-info observations.
# Requirement: Required; recommended value: 900.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.polymarket.market-info-refresh] When Always: The value must be at least 60 seconds.
# Operational impact: Changes quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
clob_market_info_refresh_secs = 900
# Field: polymarket.chain_id
# Purpose: EVM chain ID; must be Polygon (`137`) — validated at startup.
# Requirement: Required; recommended value: 137.
# Type/unit/range: Integer; chainid; minimum=0.
# Validation/cross-field contract: [deploy.polymarket.chain] When Always: The value must equal the Polygon mainnet chain ID 137.
# Operational impact: Changes quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
chain_id = 137

[polymarket.onchain]
# Field: polymarket.onchain.provider_id
# Purpose: Stable identity of the settlement/read RPC provider and failure domain.
# Requirement: Required; recommended value: "polygon_public_rpc".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-api Polygon settlement and on-chain evidence adapter; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polygon settlement and on-chain evidence adapter
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
provider_id = "polygon_public_rpc"
# Field: polymarket.onchain.rpc_timeout_ms
# Purpose: RPC request timeout (ms). Default: `10000`.
# Requirement: Required; recommended value: 10000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.polymarket.rpc-timeout] When Always: The timeout must be greater than zero.
# Operational impact: Changes quant-pivot-api Polygon settlement and on-chain evidence adapter; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polygon settlement and on-chain evidence adapter
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
rpc_timeout_ms = 10000

# Field: polymarket.onchain.rpc_endpoint
# Purpose: Typed Polygon JSON-RPC endpoint source.
# Requirement: Required; recommended value: {"source":"public","url":"https://polygon-rpc.com"}.
# Type/unit/range: Variant; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.polymarket.rpc-endpoint] When Always: The endpoint must be HTTP(S). A public endpoint may not contain user-info, path credentials, query parameters, or a fragment; authenticated endpoints must use the protected variant.
# Operational impact: Changes quant-pivot-api Polygon settlement and on-chain evidence adapter; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polygon settlement and on-chain evidence adapter
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: protected, public.
[polymarket.onchain.rpc_endpoint]
source = "protected"
url = "REPLACE_WITH_AUTHENTICATED_POLYGON_RPC_URL"

[polymarket.relayer]
# Field: polymarket.relayer.base_url
# Purpose: Relayer submit/status base URL. Default: `https://relayer-v2.polymarket.com`.
# Requirement: Required; recommended value: "https://relayer-v2.polymarket.com".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-api Polymarket relayer authentication and submission adapter; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket relayer authentication and submission adapter
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
base_url = "https://relayer-v2.polymarket.com"
# Field: polymarket.relayer.request_timeout_ms
# Purpose: HTTP request timeout (ms). Default: `15000`.
# Requirement: Required; recommended value: 15000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-api Polymarket relayer authentication and submission adapter; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket relayer authentication and submission adapter
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
request_timeout_ms = 15000

# Optional assignment in this owning table. Uncomment only after satisfying its cross-field contract.
# Field: polymarket.relayer.api_key
# Purpose: Zeroizing Polymarket relayer API key used only by the authenticated submission adapter.
# Requirement: Optional; recommended value: no implicit default.
# Type/unit/range: String; unitless; write_only.
# Validation/cross-field contract: [deploy.relayer.credentials] When When runtime mode permits order submission and wallet_kind is proxy, gnosis_safe, or deposit_wallet: Both relayer API key and relayer API-key address are required.
# Operational impact: Changes quant-pivot-api Polymarket relayer authentication and submission adapter; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket relayer authentication and submission adapter
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: secret; deployment projection never exposes protected literals.
# Variants: not applicable.
# api_key = "REPLACE_WITH_POLYMARKET_RELAYER_API_KEY"

# Optional assignment in this owning table. Uncomment only after satisfying its cross-field contract.
# Field: polymarket.relayer.api_key_address
# Purpose: Ethereum address that owns the relayer API key (the signer EOA address).
# Requirement: Optional; recommended value: no implicit default.
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.relayer.credentials] When When runtime mode permits order submission and wallet_kind is proxy, gnosis_safe, or deposit_wallet: Both relayer API key and relayer API-key address are required.
# Operational impact: Changes quant-pivot-api Polymarket relayer authentication and submission adapter; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket relayer authentication and submission adapter
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_identifier; deployment projection never exposes protected literals.
# Variants: not applicable.
# api_key_address = "REPLACE_WITH_RELAYER_SIGNER_EOA_ADDRESS"

[polymarket.settlement]
# Field: polymarket.settlement.claim_lease_secs
# Purpose: Exclusive case-claim lease duration. A crashed worker may be replaced only after this database-backed lease expires.
# Requirement: Required; recommended value: 30.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.settlement.claim-lease] When Always: The lease must be between 5 and 300 seconds inclusive.
# Operational impact: Changes quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
claim_lease_secs = 30
# Field: polymarket.settlement.operator_authorization_ttl_secs
# Purpose: Fixed TTL for an operator settlement-authorization challenge.
# Requirement: Required; recommended value: 300.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.settlement.authorization-ttl] When Always: The authorization TTL must be between 30 and 3600 seconds inclusive.
# Operational impact: Changes quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
operator_authorization_ttl_secs = 300
# Field: polymarket.settlement.discovery_poll_secs
# Purpose: Durable discovery cadence; catalog events may only wake this poll early.
# Requirement: Required; recommended value: 30.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.settlement.polls] When Always: Discovery and submission polling intervals must each be greater than zero.
# Operational impact: Changes quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
discovery_poll_secs = 30
# Field: polymarket.settlement.submission_poll_secs
# Purpose: Recovery/submission polling cadence.
# Requirement: Required; recommended value: 5.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.settlement.polls] When Always: Discovery and submission polling intervals must each be greater than zero.
# Operational impact: Changes quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
submission_poll_secs = 5
# Field: polymarket.settlement.max_claims_per_tick
# Purpose: Maximum cases processed by one worker tick.
# Requirement: Required; recommended value: 32.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.settlement.claim-budget] When Always: The per-tick claim budget must be between 1 and 1024 inclusive.
# Operational impact: Changes quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_claims_per_tick = 32
# Field: polymarket.settlement.rpc_concurrency
# Purpose: Maximum concurrent signer-free settlement RPC operations.
# Requirement: Required; recommended value: 4.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.settlement.rpc-concurrency] When Always: RPC concurrency must be between 1 and 32 inclusive.
# Operational impact: Changes quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
rpc_concurrency = 4
# Field: polymarket.settlement.readiness_ui_cache_secs
# Purpose: UI-only deployment-readiness cache TTL. Money-path preflight and admission always bypass this cache.
# Requirement: Required; recommended value: 10.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.settlement.readiness-cache] When Always: The readiness cache TTL must be between 1 and 60 seconds inclusive.
# Operational impact: Changes quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
readiness_ui_cache_secs = 10
# Field: polymarket.settlement.external_scan_block_span
# Purpose: Maximum contiguous finalized Polygon blocks read per external-evidence cursor pass.
# Requirement: Required; recommended value: 2048.
# Type/unit/range: Integer; blocks; minimum=0.
# Validation/cross-field contract: [deploy.settlement.scan-span] When Always: The external settlement scan span must be between 1 and 10000 blocks inclusive.
# Operational impact: Changes quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
external_scan_block_span = 2048
# Field: polymarket.settlement.retry_initial_secs
# Purpose: First durable retry delay before exponential backoff.
# Requirement: Required; recommended value: 2.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.settlement.retry-window] When Always: retry_initial_secs must be greater than zero and no greater than retry_max_secs; retry_max_secs must be no greater than 3600 seconds.
# Operational impact: Changes quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
retry_initial_secs = 2
# Field: polymarket.settlement.retry_max_secs
# Purpose: Maximum durable delay between settlement retry attempts after exponential backoff.
# Requirement: Required; recommended value: 300.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.settlement.retry-window] When Always: retry_initial_secs must be greater than zero and no greater than retry_max_secs; retry_max_secs must be no greater than 3600 seconds.
# Operational impact: Changes quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api Polymarket CLOB, Gamma, WebSocket, and settlement clients
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
retry_max_secs = 300

[market_data.websocket]
# Field: market_data.websocket.reconnect_delay_ms
# Purpose: Initial reconnect backoff (ms) after a dropped connection. Default: `1000`.
# Requirement: Required; recommended value: 1000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
reconnect_delay_ms = 1000
# Field: market_data.websocket.max_reconnect_delay_ms
# Purpose: Reconnect backoff ceiling (ms). Default: `30000`.
# Requirement: Required; recommended value: 30000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_reconnect_delay_ms = 30000
# Field: market_data.websocket.max_subscriptions_per_connection
# Purpose: Token subscriptions per WS connection before a new shard is opened. Affects connection count and per-connection event volume. Default: `200`.
# Requirement: Required; recommended value: 200.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.market-websocket.subscription-budget] When Always: The per-connection subscription limit, engine token limit, and subscription window must each be greater than zero.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_subscriptions_per_connection = 200
# Field: market_data.websocket.engine_max_subscription_tokens
# Purpose: Maximum active engine tokens subscribed across all WS connections. Default: `2000`.
# Requirement: Required; recommended value: 2000.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.market-websocket.subscription-budget] When Always: The per-connection subscription limit, engine token limit, and subscription window must each be greater than zero.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
engine_max_subscription_tokens = 2000
# Field: market_data.websocket.engine_subscription_window_hours
# Purpose: Look-ahead window (hours) for engine WS subscription subscription. Default: `72`.
# Requirement: Required; recommended value: 72.
# Type/unit/range: Integer; hours; minimum=0.
# Validation/cross-field contract: [deploy.market-websocket.subscription-budget] When Always: The per-connection subscription limit, engine token limit, and subscription window must each be greater than zero.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
engine_subscription_window_hours = 72

[market_data.gamma]
# Field: market_data.gamma.base_url
# Purpose: Gamma REST base URL. Default: `https://gamma-api.polymarket.com`.
# Requirement: Required; recommended value: "https://gamma-api.polymarket.com".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
base_url = "https://gamma-api.polymarket.com"
# Field: market_data.gamma.reconcile_interval_secs
# Purpose: Interval between complete keyset reconciliations. Default: `300` seconds.
# Requirement: Required; recommended value: 300.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.gamma.pagination] When Always: All values must be greater than zero; page_size must be no greater than the Gamma /events/keyset limit of 500; max_keyset_requests must be no less than max_keyset_pages; historical identity must cover the exchange-history retention frontier.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
reconcile_interval_secs = 300
# Field: market_data.gamma.page_size
# Purpose: Page size for catalog pagination. Default: `100`.
# Requirement: Required; recommended value: 100.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.gamma.pagination] When Always: All values must be greater than zero; page_size must be no greater than the Gamma /events/keyset limit of 500; max_keyset_requests must be no less than max_keyset_pages; historical identity must cover the exchange-history retention frontier.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
page_size = 100
# Field: market_data.gamma.max_keyset_pages
# Purpose: Maximum successful keyset pages in one scan. Default: `10_000`.
# Requirement: Required; recommended value: 10000.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.gamma.pagination] When Always: All values must be greater than zero; page_size must be no greater than the Gamma /events/keyset limit of 500; max_keyset_requests must be no less than max_keyset_pages; historical identity must cover the exchange-history retention frontier.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_keyset_pages = 10000
# Field: market_data.gamma.max_keyset_requests
# Purpose: Maximum HTTP attempts, including retries, in one scan. Default: `50_000`.
# Requirement: Required; recommended value: 50000.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.gamma.pagination] When Always: All values must be greater than zero; page_size must be no greater than the Gamma /events/keyset limit of 500; max_keyset_requests must be no less than max_keyset_pages; historical identity must cover the exchange-history retention frontier.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_keyset_requests = 50000
# Field: market_data.gamma.historical_identity_days
# Purpose: Closed-event identity horizon required by fresh-boot history projection. Default: `200` days.
# Requirement: Required; recommended value: 200.
# Type/unit/range: Integer; days; minimum=0.
# Validation/cross-field contract: [deploy.gamma.pagination] When Always: All values must be greater than zero; page_size must be no greater than the Gamma /events/keyset limit of 500; max_keyset_requests must be no less than max_keyset_pages; historical identity must cover the exchange-history retention frontier.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_identity_days = 200

[market_data.data_api]
# Field: market_data.data_api.base_url
# Purpose: Data API base URL. Default: `https://data-api.polymarket.com`.
# Requirement: Required; recommended value: "https://data-api.polymarket.com".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
base_url = "https://data-api.polymarket.com"
# Field: market_data.data_api.page_size
# Purpose: Page size for positions pagination (`1..=500`). Default: `500`.
# Requirement: Required; recommended value: 500.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
page_size = 500
# Field: market_data.data_api.size_threshold
# Purpose: Minimum token size to include (`sizeThreshold`). Default: `1`.
# Requirement: Required; recommended value: 1.
# Type/unit/range: Integer; shares; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
size_threshold = 1

[market_data.finalized_exchange_history]
# Field: market_data.finalized_exchange_history.enabled
# Purpose: Enable finalized history extraction and projection.
# Requirement: Required; recommended value: false.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.exchange-history.authority] When Always: Enabled history requires authenticated HyperSync extraction, an independent non-Envio archive RPC witness, and the fixed fresh-boot PIT/frontier policy.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
enabled = false
# Field: market_data.finalized_exchange_history.poll_secs
# Purpose: Poll interval after the activation frontier reaches the finalized head.
# Requirement: Required; recommended value: 30.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
poll_secs = 30
# Field: market_data.finalized_exchange_history.connect_timeout_ms
# Purpose: HTTP connect timeout in milliseconds.
# Requirement: Required; recommended value: 10000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.exchange-history.work-budget] When Always: Timeout, attestor request span/concurrency, response-byte, adaptive logical-chunk, retry, worker, and batch budgets must form valid bounded ranges.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
connect_timeout_ms = 10000
# Field: market_data.finalized_exchange_history.request_timeout_ms
# Purpose: Per-request timeout in milliseconds.
# Requirement: Required; recommended value: 60000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.exchange-history.work-budget] When Always: Timeout, attestor request span/concurrency, response-byte, adaptive logical-chunk, retry, worker, and batch budgets must form valid bounded ranges.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
request_timeout_ms = 60000
# Field: market_data.finalized_exchange_history.max_response_bytes
# Purpose: Maximum decoded response body accepted from either provider.
# Requirement: Required; recommended value: 67108864.
# Type/unit/range: Integer; bytes; minimum=0.
# Validation/cross-field contract: [deploy.exchange-history.work-budget] When Always: Timeout, attestor request span/concurrency, response-byte, adaptive logical-chunk, retry, worker, and batch budgets must form valid bounded ranges.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_response_bytes = 67108864
# Field: market_data.finalized_exchange_history.min_blocks_per_chunk
# Purpose: Minimum inclusive chunk span after adaptive contraction.
# Requirement: Required; recommended value: 100.
# Type/unit/range: Integer; blocks; minimum=0.
# Validation/cross-field contract: [deploy.exchange-history.work-budget] When Always: Timeout, attestor request span/concurrency, response-byte, adaptive logical-chunk, retry, worker, and batch budgets must form valid bounded ranges.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
min_blocks_per_chunk = 100
# Field: market_data.finalized_exchange_history.max_blocks_per_chunk
# Purpose: Maximum inclusive chunk span after adaptive expansion.
# Requirement: Required; recommended value: 2000.
# Type/unit/range: Integer; blocks; minimum=0.
# Validation/cross-field contract: [deploy.exchange-history.work-budget] When Always: Timeout, attestor request span/concurrency, response-byte, adaptive logical-chunk, retry, worker, and batch budgets must form valid bounded ranges.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_blocks_per_chunk = 2000
# Field: market_data.finalized_exchange_history.retry_initial_ms
# Purpose: First retry delay in milliseconds.
# Requirement: Required; recommended value: 500.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.exchange-history.work-budget] When Always: Timeout, attestor request span/concurrency, response-byte, adaptive logical-chunk, retry, worker, and batch budgets must form valid bounded ranges.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
retry_initial_ms = 500
# Field: market_data.finalized_exchange_history.retry_max_ms
# Purpose: Maximum retry delay in milliseconds.
# Requirement: Required; recommended value: 30000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.exchange-history.work-budget] When Always: Timeout, attestor request span/concurrency, response-byte, adaptive logical-chunk, retry, worker, and batch budgets must form valid bounded ranges.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
retry_max_ms = 30000
# Field: market_data.finalized_exchange_history.retry_max_attempts
# Purpose: Maximum attempts per provider pair before the frontier fails closed.
# Requirement: Required; recommended value: 8.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.exchange-history.work-budget] When Always: Timeout, attestor request span/concurrency, response-byte, adaptive logical-chunk, retry, worker, and batch budgets must form valid bounded ranges.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
retry_max_attempts = 8
# Field: market_data.finalized_exchange_history.model_confirmation_blocks
# Purpose: Confirmation delay used to reconstruct `model_available_at`.
# Requirement: Required; recommended value: 12.
# Type/unit/range: Integer; blocks; minimum=0.
# Validation/cross-field contract: [deploy.exchange-history.authority] When Always: Enabled history requires authenticated HyperSync extraction, an independent non-Envio archive RPC witness, and the fixed fresh-boot PIT/frontier policy.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
model_confirmation_blocks = 12
# Field: market_data.finalized_exchange_history.rollback_buffer_blocks
# Purpose: Reconciliation buffer behind the accepted frontier.
# Requirement: Required; recommended value: 200.
# Type/unit/range: Integer; blocks; minimum=0.
# Validation/cross-field contract: [deploy.exchange-history.authority] When Always: Enabled history requires authenticated HyperSync extraction, an independent non-Envio archive RPC witness, and the fixed fresh-boot PIT/frontier policy.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
rollback_buffer_blocks = 200
# Field: market_data.finalized_exchange_history.activation_frontier_days
# Purpose: Recent history window prioritized for the first pooled activation.
# Requirement: Required; recommended value: 33.
# Type/unit/range: Integer; days; minimum=0.
# Validation/cross-field contract: [deploy.exchange-history.authority] When Always: Enabled history requires authenticated HyperSync extraction, an independent non-Envio archive RPC witness, and the fixed fresh-boot PIT/frontier policy.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
activation_frontier_days = 33
# Field: market_data.finalized_exchange_history.retention_frontier_days
# Purpose: Long-term raw history target filled after activation.
# Requirement: Required; recommended value: 200.
# Type/unit/range: Integer; days; minimum=0.
# Validation/cross-field contract: [deploy.exchange-history.authority] When Always: Enabled history requires authenticated HyperSync extraction, an independent non-Envio archive RPC witness, and the fixed fresh-boot PIT/frontier policy.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
retention_frontier_days = 200
# Field: market_data.finalized_exchange_history.hot_window_blocks_per_tick
# Purpose: Maximum blocks assigned to the activation frontier per scheduling turn.
# Requirement: Required; recommended value: 50000.
# Type/unit/range: Integer; blocks; minimum=0.
# Validation/cross-field contract: [deploy.exchange-history.work-budget] When Always: Timeout, attestor request span/concurrency, response-byte, adaptive logical-chunk, retry, worker, and batch budgets must form valid bounded ranges.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
hot_window_blocks_per_tick = 50000
# Field: market_data.finalized_exchange_history.full_history_blocks_per_tick
# Purpose: Maximum blocks assigned to the retention frontier per scheduling turn.
# Requirement: Required; recommended value: 5000.
# Type/unit/range: Integer; blocks; minimum=0.
# Validation/cross-field contract: [deploy.exchange-history.work-budget] When Always: Timeout, attestor request span/concurrency, response-byte, adaptive logical-chunk, retry, worker, and batch budgets must form valid bounded ranges.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
full_history_blocks_per_tick = 5000
# Field: market_data.finalized_exchange_history.batch_size
# Purpose: Maximum rows written in one fact batch.
# Requirement: Required; recommended value: 1000.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.exchange-history.work-budget] When Always: Timeout, attestor request span/concurrency, response-byte, adaptive logical-chunk, retry, worker, and batch budgets must form valid bounded ranges.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
batch_size = 1000

[market_data.finalized_exchange_history.hypersync]
# Field: market_data.finalized_exchange_history.hypersync.provider_id
# Purpose: Stable identity of the primary extraction provider and failure domain.
# Requirement: Required; recommended value: "envio_hypersync_polygon".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
provider_id = "envio_hypersync_polygon"
# Field: market_data.finalized_exchange_history.hypersync.endpoint
# Purpose: Primary finalized Polygon exchange-history extraction endpoint.
# Requirement: Required; recommended value: "https://polygon.hypersync.xyz".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.exchange-history.authority] When Always: Enabled history requires authenticated HyperSync extraction, an independent non-Envio archive RPC witness, and the fixed fresh-boot PIT/frontier policy.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
endpoint = "https://polygon.hypersync.xyz"
# Field: market_data.finalized_exchange_history.hypersync.api_token
# Purpose: Bearer credential used only by the primary historical extraction adapter.
# Requirement: Required; recommended value: "".
# Type/unit/range: String; unitless; write_only.
# Validation/cross-field contract: [deploy.exchange-history.authority] When Always: Enabled history requires authenticated HyperSync extraction, an independent non-Envio archive RPC witness, and the fixed fresh-boot PIT/frontier policy.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: secret; deployment projection never exposes protected literals.
# Variants: not applicable.
api_token = ""

[market_data.finalized_exchange_history.attestor]
# Field: market_data.finalized_exchange_history.attestor.provider_id
# Purpose: Stable identity of the independent archive witness provider.
# Requirement: Required; recommended value: "publicnode_polygon_archive".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
provider_id = "publicnode_polygon_archive"
# Field: market_data.finalized_exchange_history.attestor.max_blocks_per_log_request
# Purpose: Maximum inclusive block span in one `eth_getLogs` request.
# Requirement: Required; recommended value: 10.
# Type/unit/range: Integer; blocks; minimum=0.
# Validation/cross-field contract: [deploy.exchange-history.work-budget] When Always: Timeout, attestor request span/concurrency, response-byte, adaptive logical-chunk, retry, worker, and batch budgets must form valid bounded ranges.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_blocks_per_log_request = 10
# Field: market_data.finalized_exchange_history.attestor.max_concurrent_log_requests
# Purpose: Maximum concurrent `eth_getLogs` subrequests within one logical chunk.
# Requirement: Required; recommended value: 2.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.exchange-history.work-budget] When Always: Timeout, attestor request span/concurrency, response-byte, adaptive logical-chunk, retry, worker, and batch budgets must form valid bounded ranges.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_concurrent_log_requests = 2

# Field: market_data.finalized_exchange_history.attestor.rpc_endpoint
# Purpose: JSON-RPC endpoint in a trust and failure domain independent of Envio.
# Requirement: Required; recommended value: {"source":"public","url":"https://polygon-bor-rpc.publicnode.com"}.
# Type/unit/range: Variant; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.exchange-history.authority] When Always: Enabled history requires authenticated HyperSync extraction, an independent non-Envio archive RPC witness, and the fixed fresh-boot PIT/frontier policy.
# Operational impact: Changes quant-pivot-core market-data ingestion, durability, and reconciliation pipeline; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core market-data ingestion, durability, and reconciliation pipeline
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: protected, public.
[market_data.finalized_exchange_history.attestor.rpc_endpoint]
source = "public"
url = "https://polygon-bor-rpc.publicnode.com"

[domain_sources.binance]
# Field: domain_sources.binance.enabled
# Purpose: Controls startup and scheduling of this Binance market-data source.
# Requirement: Required; recommended value: true.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.binance] When domain_sources.binance.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
enabled = true
# Field: domain_sources.binance.rest_url
# Purpose: REST API root used for klines, exchange time, and bounded recovery reads.
# Requirement: Required; recommended value: "https://api.binance.com".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.binance] When domain_sources.binance.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
rest_url = "https://api.binance.com"
# Field: domain_sources.binance.websocket_url
# Purpose: WebSocket endpoint used for live aggregate-trade ingestion.
# Requirement: Required; recommended value: "wss://stream.binance.com:9443/ws".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.binance] When domain_sources.binance.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
websocket_url = "wss://stream.binance.com:9443/ws"
# Field: domain_sources.binance.archive_url
# Purpose: Official Binance bulk-data archive root used for historical PIT backfill.
# Requirement: Required; recommended value: "https://data.binance.vision".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.binance] When domain_sources.binance.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
archive_url = "https://data.binance.vision"
# Field: domain_sources.binance.weight_budget_per_min
# Purpose: Maximum Binance request weight admitted per rolling minute across this source client.
# Requirement: Required; recommended value: 1000.
# Type/unit/range: Integer; requestweightperminute; minimum=0.
# Validation/cross-field contract: [deploy.source.binance] When domain_sources.binance.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
weight_budget_per_min = 1000
# Field: domain_sources.binance.kline_poll_secs
# Purpose: Poll cadence for the latest closed kline after live-stream recovery.
# Requirement: Required; recommended value: 30.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.binance] When domain_sources.binance.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
kline_poll_secs = 30
# Field: domain_sources.binance.agg_trade_recovery_poll_secs
# Purpose: Poll cadence for bounded aggregate-trade gap recovery.
# Requirement: Required; recommended value: 5.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.binance] When domain_sources.binance.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
agg_trade_recovery_poll_secs = 5
# Field: domain_sources.binance.websocket_rotation_secs
# Purpose: Maximum lifetime of one WebSocket session before proactive rotation prevents server expiry.
# Requirement: Required; recommended value: 82800.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.binance] When domain_sources.binance.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
websocket_rotation_secs = 82800
# Field: domain_sources.binance.batch_size
# Purpose: Maximum records decoded and persisted by one bounded recovery batch.
# Requirement: Required; recommended value: 5000.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.source.binance] When domain_sources.binance.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
batch_size = 5000
# Field: domain_sources.binance.request_timeout_ms
# Purpose: End-to-end timeout for one Binance REST request.
# Requirement: Required; recommended value: 10000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.source.binance] When domain_sources.binance.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
request_timeout_ms = 10000
# Field: domain_sources.binance.max_clock_skew_ms
# Purpose: Maximum trusted difference between the local midpoint clock and Binance `GET /api/v3/time`.
# Requirement: Required; recommended value: 2000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.source.binance] When domain_sources.binance.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_clock_skew_ms = 2000

[domain_sources.binance_usdm_futures]
# Field: domain_sources.binance_usdm_futures.enabled
# Purpose: Controls startup and scheduling of this Binance market-data source.
# Requirement: Required; recommended value: true.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.binance-usdm] When domain_sources.binance_usdm_futures.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
enabled = true
# Field: domain_sources.binance_usdm_futures.rest_url
# Purpose: REST API root used for klines, exchange time, and bounded recovery reads.
# Requirement: Required; recommended value: "https://fapi.binance.com".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.binance-usdm] When domain_sources.binance_usdm_futures.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
rest_url = "https://fapi.binance.com"
# Field: domain_sources.binance_usdm_futures.websocket_url
# Purpose: WebSocket endpoint used for live aggregate-trade ingestion.
# Requirement: Required; recommended value: "wss://fstream.binance.com/ws".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.binance-usdm] When domain_sources.binance_usdm_futures.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
websocket_url = "wss://fstream.binance.com/ws"
# Field: domain_sources.binance_usdm_futures.archive_url
# Purpose: Official Binance bulk-data archive root used for historical PIT backfill.
# Requirement: Required; recommended value: "https://data.binance.vision".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.binance-usdm] When domain_sources.binance_usdm_futures.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
archive_url = "https://data.binance.vision"
# Field: domain_sources.binance_usdm_futures.weight_budget_per_min
# Purpose: Maximum Binance request weight admitted per rolling minute across this source client.
# Requirement: Required; recommended value: 1000.
# Type/unit/range: Integer; requestweightperminute; minimum=0.
# Validation/cross-field contract: [deploy.source.binance-usdm] When domain_sources.binance_usdm_futures.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
weight_budget_per_min = 1000
# Field: domain_sources.binance_usdm_futures.kline_poll_secs
# Purpose: Poll cadence for the latest closed kline after live-stream recovery.
# Requirement: Required; recommended value: 30.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.binance-usdm] When domain_sources.binance_usdm_futures.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
kline_poll_secs = 30
# Field: domain_sources.binance_usdm_futures.agg_trade_recovery_poll_secs
# Purpose: Poll cadence for bounded aggregate-trade gap recovery.
# Requirement: Required; recommended value: 5.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.binance-usdm] When domain_sources.binance_usdm_futures.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
agg_trade_recovery_poll_secs = 5
# Field: domain_sources.binance_usdm_futures.websocket_rotation_secs
# Purpose: Maximum lifetime of one WebSocket session before proactive rotation prevents server expiry.
# Requirement: Required; recommended value: 82800.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.binance-usdm] When domain_sources.binance_usdm_futures.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
websocket_rotation_secs = 82800
# Field: domain_sources.binance_usdm_futures.batch_size
# Purpose: Maximum records decoded and persisted by one bounded recovery batch.
# Requirement: Required; recommended value: 5000.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.source.binance-usdm] When domain_sources.binance_usdm_futures.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
batch_size = 5000
# Field: domain_sources.binance_usdm_futures.request_timeout_ms
# Purpose: End-to-end timeout for one Binance REST request.
# Requirement: Required; recommended value: 10000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.source.binance-usdm] When domain_sources.binance_usdm_futures.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
request_timeout_ms = 10000
# Field: domain_sources.binance_usdm_futures.max_clock_skew_ms
# Purpose: Maximum trusted difference between the local midpoint clock and Binance `GET /api/v3/time`.
# Requirement: Required; recommended value: 2000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.source.binance-usdm] When domain_sources.binance_usdm_futures.enabled is true: request_timeout_ms, agg_trade_recovery_poll_secs, websocket_rotation_secs, batch_size, and max_clock_skew_ms must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_clock_skew_ms = 2000

[domain_sources.polymarket_rtds]
# Field: domain_sources.polymarket_rtds.enabled
# Purpose: Controls startup and subscription of the public Polymarket RTDS adapter.
# Requirement: Required; recommended value: true.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.polymarket-rtds] When domain_sources.polymarket_rtds.enabled is true: websocket_url must use ws/wss with a host, connect_timeout_ms and max_clock_skew_ms must be greater than zero, and keepalive_secs must equal the official 5-second cadence.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
enabled = true
# Field: domain_sources.polymarket_rtds.websocket_url
# Purpose: Official RTDS WebSocket endpoint used for source-native crypto observations.
# Requirement: Required; recommended value: "wss://ws-live-data.polymarket.com".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.polymarket-rtds] When domain_sources.polymarket_rtds.enabled is true: websocket_url must use ws/wss with a host, connect_timeout_ms and max_clock_skew_ms must be greater than zero, and keepalive_secs must equal the official 5-second cadence.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
websocket_url = "wss://ws-live-data.polymarket.com"
# Field: domain_sources.polymarket_rtds.connect_timeout_ms
# Purpose: Maximum time allowed to establish one RTDS WebSocket session.
# Requirement: Required; recommended value: 10000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.source.polymarket-rtds] When domain_sources.polymarket_rtds.enabled is true: websocket_url must use ws/wss with a host, connect_timeout_ms and max_clock_skew_ms must be greater than zero, and keepalive_secs must equal the official 5-second cadence.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
connect_timeout_ms = 10000
# Field: domain_sources.polymarket_rtds.keepalive_secs
# Purpose: Official protocol heartbeat cadence. The RTDS documentation requires a text `PING` every five seconds.
# Requirement: Required; recommended value: 5.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.polymarket-rtds] When domain_sources.polymarket_rtds.enabled is true: websocket_url must use ws/wss with a host, connect_timeout_ms and max_clock_skew_ms must be greater than zero, and keepalive_secs must equal the official 5-second cadence.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
keepalive_secs = 5
# Field: domain_sources.polymarket_rtds.max_clock_skew_ms
# Purpose: Maximum accepted difference between RTDS event time and the local midpoint clock.
# Requirement: Required; recommended value: 30000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.source.polymarket-rtds] When domain_sources.polymarket_rtds.enabled is true: websocket_url must use ws/wss with a host, connect_timeout_ms and max_clock_skew_ms must be greater than zero, and keepalive_secs must equal the official 5-second cadence.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_clock_skew_ms = 30000

[domain_sources.chainlink_data_streams]
# Field: domain_sources.chainlink_data_streams.enabled
# Purpose: A missing subscription is valid for unrelated reports. Any bound condition still fails closed at preflight/evaluation.
# Requirement: Required; recommended value: false.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.chainlink-streams] When domain_sources.chainlink_data_streams.enabled is true: API key and API secret must both be present, at least one frozen V3 feed must exist, and max_clock_skew_ms and rest_page_limit must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
enabled = false
# Field: domain_sources.chainlink_data_streams.rest_url
# Purpose: REST API root used for authenticated report recovery and pagination.
# Requirement: Required; recommended value: "https://api.dataengine.chain.link".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.chainlink-streams] When domain_sources.chainlink_data_streams.enabled is true: API key and API secret must both be present, at least one frozen V3 feed must exist, and max_clock_skew_ms and rest_page_limit must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
rest_url = "https://api.dataengine.chain.link"
# Field: domain_sources.chainlink_data_streams.websocket_url
# Purpose: WebSocket endpoint used for authenticated live report delivery.
# Requirement: Required; recommended value: "wss://ws.dataengine.chain.link".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.chainlink-streams] When domain_sources.chainlink_data_streams.enabled is true: API key and API secret must both be present, at least one frozen V3 feed must exist, and max_clock_skew_ms and rest_page_limit must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
websocket_url = "wss://ws.dataengine.chain.link"
# Field: domain_sources.chainlink_data_streams.max_clock_skew_ms
# Purpose: Maximum accepted difference between signed report time and the local midpoint clock.
# Requirement: Required; recommended value: 2000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.source.chainlink-streams] When domain_sources.chainlink_data_streams.enabled is true: API key and API secret must both be present, at least one frozen V3 feed must exist, and max_clock_skew_ms and rest_page_limit must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_clock_skew_ms = 2000
# Field: domain_sources.chainlink_data_streams.rest_page_limit
# Purpose: Maximum reports requested in one bounded REST recovery page.
# Requirement: Required; recommended value: 1000.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.source.chainlink-streams] When domain_sources.chainlink_data_streams.enabled is true: API key and API secret must both be present, at least one frozen V3 feed must exist, and max_clock_skew_ms and rest_page_limit must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
rest_page_limit = 1000

# Optional assignment in this owning table. Uncomment only after satisfying its cross-field contract.
# Field: domain_sources.chainlink_data_streams.api_key
# Purpose: Subscription API key paired atomically with `api_secret`.
# Requirement: Optional; recommended value: no implicit default.
# Type/unit/range: String; unitless; write_only.
# Validation/cross-field contract: [deploy.source.chainlink-streams] When domain_sources.chainlink_data_streams.enabled is true: API key and API secret must both be present, at least one frozen V3 feed must exist, and max_clock_skew_ms and rest_page_limit must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: secret; deployment projection never exposes protected literals.
# Variants: not applicable.
# api_key = "REPLACE_WITH_DOMAIN_SOURCES_CHAINLINK_DATA_STREAMS_API_KEY"

# Optional assignment in this owning table. Uncomment only after satisfying its cross-field contract.
# Field: domain_sources.chainlink_data_streams.api_secret
# Purpose: Subscription API secret paired atomically with `api_key`.
# Requirement: Optional; recommended value: no implicit default.
# Type/unit/range: String; unitless; write_only.
# Validation/cross-field contract: [deploy.source.chainlink-streams] When domain_sources.chainlink_data_streams.enabled is true: API key and API secret must both be present, at least one frozen V3 feed must exist, and max_clock_skew_ms and rest_page_limit must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: secret; deployment projection never exposes protected literals.
# Variants: not applicable.
# api_secret = "REPLACE_WITH_DOMAIN_SOURCES_CHAINLINK_DATA_STREAMS_API_SECRET"

# Canonical dynamic binding. Uncomment this table header and every required assignment in the block as one unit.
# [domain_sources.chainlink_data_streams.feeds."example"]
# Field: domain_sources.chainlink_data_streams.feeds.*.decimals
# Purpose: Decimal scale declared by the subscribed feed metadata.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: no implicit default.
# Type/unit/range: Integer; decimalplaces; minimum=0.
# Validation/cross-field contract: [deploy.source.chainlink-streams] When domain_sources.chainlink_data_streams.enabled is true: API key and API secret must both be present, at least one frozen V3 feed must exist, and max_clock_skew_ms and rest_page_limit must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
# decimals = 8
# Field: domain_sources.chainlink_data_streams.feeds.*.feed_id
# Purpose: Immutable Chainlink Data Streams V3 feed identifier from the active subscription.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: no implicit default.
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.chainlink-streams] When domain_sources.chainlink_data_streams.enabled is true: API key and API secret must both be present, at least one frozen V3 feed must exist, and max_clock_skew_ms and rest_page_limit must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
# feed_id = "REPLACE_WITH_SUBSCRIPTION_V3_FEED_ID"

[domain_sources.chainlink_data_streams.feeds]

[domain_sources.aviation_weather]
# Field: domain_sources.aviation_weather.enabled
# Purpose: Controls startup and scheduling of Aviation Weather station ingestion.
# Requirement: Required; recommended value: true.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.aviation-weather] When domain_sources.aviation_weather.enabled is true: poll_secs and request_timeout_ms must each be greater than zero, and day_close_grace_secs must equal the governed 7200-second methodology value.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
enabled = true
# Field: domain_sources.aviation_weather.base_url
# Purpose: Official Aviation Weather API root used for METAR station observations.
# Requirement: Required; recommended value: "https://aviationweather.gov/api/data".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.aviation-weather] When domain_sources.aviation_weather.enabled is true: poll_secs and request_timeout_ms must each be greater than zero, and day_close_grace_secs must equal the governed 7200-second methodology value.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
base_url = "https://aviationweather.gov/api/data"
# Field: domain_sources.aviation_weather.poll_secs
# Purpose: Poll cadence for the latest METAR station observations.
# Requirement: Required; recommended value: 60.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.aviation-weather] When domain_sources.aviation_weather.enabled is true: poll_secs and request_timeout_ms must each be greater than zero, and day_close_grace_secs must equal the governed 7200-second methodology value.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
poll_secs = 60
# Field: domain_sources.aviation_weather.request_timeout_ms
# Purpose: End-to-end timeout for one Aviation Weather HTTP request.
# Requirement: Required; recommended value: 10000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.source.aviation-weather] When domain_sources.aviation_weather.enabled is true: poll_secs and request_timeout_ms must each be greater than zero, and day_close_grace_secs must equal the governed 7200-second methodology value.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
request_timeout_ms = 10000
# Field: domain_sources.aviation_weather.day_close_grace_secs
# Purpose: Delay after station-local midnight before emitting NOAA observation-day close. This is not Wunderground settlement finalization.
# Requirement: Required; recommended value: 7200.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.aviation-weather] When domain_sources.aviation_weather.enabled is true: poll_secs and request_timeout_ms must each be greater than zero, and day_close_grace_secs must equal the governed 7200-second methodology value.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
day_close_grace_secs = 7200

[domain_sources.ghcnh]
# Field: domain_sources.ghcnh.enabled
# Purpose: Controls startup and scheduling of NOAA `GHCNh` calibration ingestion.
# Requirement: Required; recommended value: true.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.ghcnh] When domain_sources.ghcnh.enabled is true: base_url must be HTTP(S); request_timeout_ms, refresh_secs, and calibration_years must each be greater than zero; max_concurrency must be between 1 and 8 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
enabled = true
# Field: domain_sources.ghcnh.base_url
# Purpose: Official NOAA `GHCNh` by-year archive root.
# Requirement: Required; recommended value: "https://www.ncei.noaa.gov/oa/global-historical-climatology-network/hourly/access/by-year".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.ghcnh] When domain_sources.ghcnh.enabled is true: base_url must be HTTP(S); request_timeout_ms, refresh_secs, and calibration_years must each be greater than zero; max_concurrency must be between 1 and 8 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
base_url = "https://www.ncei.noaa.gov/oa/global-historical-climatology-network/hourly/access/by-year"
# Field: domain_sources.ghcnh.request_timeout_ms
# Purpose: End-to-end timeout for one `GHCNh` archive request.
# Requirement: Required; recommended value: 120000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.source.ghcnh] When domain_sources.ghcnh.enabled is true: base_url must be HTTP(S); request_timeout_ms, refresh_secs, and calibration_years must each be greater than zero; max_concurrency must be between 1 and 8 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
request_timeout_ms = 120000
# Field: domain_sources.ghcnh.refresh_secs
# Purpose: Refresh cadence for the active calibration-year window.
# Requirement: Required; recommended value: 86400.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.ghcnh] When domain_sources.ghcnh.enabled is true: base_url must be HTTP(S); request_timeout_ms, refresh_secs, and calibration_years must each be greater than zero; max_concurrency must be between 1 and 8 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
refresh_secs = 86400
# Field: domain_sources.ghcnh.calibration_years
# Purpose: Number of complete prior years loaded for station calibration and bias estimation.
# Requirement: Required; recommended value: 2.
# Type/unit/range: Integer; years; minimum=0, maximum=255.
# Validation/cross-field contract: [deploy.source.ghcnh] When domain_sources.ghcnh.enabled is true: base_url must be HTTP(S); request_timeout_ms, refresh_secs, and calibration_years must each be greater than zero; max_concurrency must be between 1 and 8 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
calibration_years = 2
# Field: domain_sources.ghcnh.max_concurrency
# Purpose: Maximum station/year `GHCNh` objects fetched concurrently.
# Requirement: Required; recommended value: 2.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.source.ghcnh] When domain_sources.ghcnh.enabled is true: base_url must be HTTP(S); request_timeout_ms, refresh_secs, and calibration_years must each be greater than zero; max_concurrency must be between 1 and 8 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_concurrency = 2

[domain_sources.ghcnd]
# Field: domain_sources.ghcnd.enabled
# Purpose: Controls startup and scheduling of NOAA `GHCNd` truth ingestion.
# Requirement: Required; recommended value: true.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.ghcnd] When domain_sources.ghcnd.enabled is true: base_url must be HTTP(S); request_timeout_ms, refresh_secs, and lookback_years must each be greater than zero; max_concurrency must be between 1 and 8 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
enabled = true
# Field: domain_sources.ghcnd.base_url
# Purpose: Official NOAA `GHCNd` station-file archive root.
# Requirement: Required; recommended value: "https://www.ncei.noaa.gov/pub/data/ghcn/daily".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.ghcnd] When domain_sources.ghcnd.enabled is true: base_url must be HTTP(S); request_timeout_ms, refresh_secs, and lookback_years must each be greater than zero; max_concurrency must be between 1 and 8 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
base_url = "https://www.ncei.noaa.gov/pub/data/ghcn/daily"
# Field: domain_sources.ghcnd.request_timeout_ms
# Purpose: End-to-end timeout for one `GHCNd` archive request.
# Requirement: Required; recommended value: 120000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.source.ghcnd] When domain_sources.ghcnd.enabled is true: base_url must be HTTP(S); request_timeout_ms, refresh_secs, and lookback_years must each be greater than zero; max_concurrency must be between 1 and 8 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
request_timeout_ms = 120000
# Field: domain_sources.ghcnd.refresh_secs
# Purpose: Refresh cadence for the active archive-quality truth window.
# Requirement: Required; recommended value: 86400.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.ghcnd] When domain_sources.ghcnd.enabled is true: base_url must be HTTP(S); request_timeout_ms, refresh_secs, and lookback_years must each be greater than zero; max_concurrency must be between 1 and 8 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
refresh_secs = 86400
# Field: domain_sources.ghcnd.lookback_years
# Purpose: Number of complete prior years retained when building archive-quality daily truth labels.
# Requirement: Required; recommended value: 2.
# Type/unit/range: Integer; years; minimum=0, maximum=255.
# Validation/cross-field contract: [deploy.source.ghcnd] When domain_sources.ghcnd.enabled is true: base_url must be HTTP(S); request_timeout_ms, refresh_secs, and lookback_years must each be greater than zero; max_concurrency must be between 1 and 8 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
lookback_years = 2
# Field: domain_sources.ghcnd.max_concurrency
# Purpose: Maximum station `GHCNd` objects fetched concurrently.
# Requirement: Required; recommended value: 2.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.source.ghcnd] When domain_sources.ghcnd.enabled is true: base_url must be HTTP(S); request_timeout_ms, refresh_secs, and lookback_years must each be greater than zero; max_concurrency must be between 1 and 8 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_concurrency = 2

[domain_sources.gefs]
# Field: domain_sources.gefs.enabled
# Purpose: Controls startup and scheduling of NOAA GEFS forecast ingestion.
# Requirement: Required; recommended value: true.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.gefs] When domain_sources.gefs.enabled is true: request_timeout_ms, poll_secs, publication_lag_secs, and max_concurrency must each be greater than zero; max_lead_hours must be a multiple of 3 between 3 and 240 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
enabled = true
# Field: domain_sources.gefs.bucket_url
# Purpose: Official NOAA GEFS object-store root used to resolve cycle products.
# Requirement: Required; recommended value: "https://noaa-gefs-pds.s3.amazonaws.com".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.gefs] When domain_sources.gefs.enabled is true: request_timeout_ms, poll_secs, publication_lag_secs, and max_concurrency must each be greater than zero; max_lead_hours must be a multiple of 3 between 3 and 240 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
bucket_url = "https://noaa-gefs-pds.s3.amazonaws.com"
# Field: domain_sources.gefs.request_timeout_ms
# Purpose: End-to-end timeout for one GEFS object request.
# Requirement: Required; recommended value: 30000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.source.gefs] When domain_sources.gefs.enabled is true: request_timeout_ms, poll_secs, publication_lag_secs, and max_concurrency must each be greater than zero; max_lead_hours must be a multiple of 3 between 3 and 240 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
request_timeout_ms = 30000
# Field: domain_sources.gefs.poll_secs
# Purpose: Poll cadence for newly available GEFS forecast cycles.
# Requirement: Required; recommended value: 900.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.gefs] When domain_sources.gefs.enabled is true: request_timeout_ms, poll_secs, publication_lag_secs, and max_concurrency must each be greater than zero; max_lead_hours must be a multiple of 3 between 3 and 240 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
poll_secs = 900
# Field: domain_sources.gefs.publication_lag_secs
# Purpose: Conservative delay after nominal cycle time before a GEFS publication may be treated as available.
# Requirement: Required; recommended value: 18000.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.gefs] When domain_sources.gefs.enabled is true: request_timeout_ms, poll_secs, publication_lag_secs, and max_concurrency must each be greater than zero; max_lead_hours must be a multiple of 3 between 3 and 240 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
publication_lag_secs = 18000
# Field: domain_sources.gefs.max_lead_hours
# Purpose: Furthest forecast lead admitted into the PIT feature plane.
# Requirement: Required; recommended value: 240.
# Type/unit/range: Integer; hours; minimum=0, maximum=65535.
# Validation/cross-field contract: [deploy.source.gefs] When domain_sources.gefs.enabled is true: request_timeout_ms, poll_secs, publication_lag_secs, and max_concurrency must each be greater than zero; max_lead_hours must be a multiple of 3 between 3 and 240 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_lead_hours = 240
# Field: domain_sources.gefs.max_concurrency
# Purpose: Maximum GEFS cycle objects fetched and decoded concurrently.
# Requirement: Required; recommended value: 8.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.source.gefs] When domain_sources.gefs.enabled is true: request_timeout_ms, poll_secs, publication_lag_secs, and max_concurrency must each be greater than zero; max_lead_hours must be a multiple of 3 between 3 and 240 inclusive.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_concurrency = 8

[domain_sources.hko_open_data]
# Field: domain_sources.hko_open_data.enabled
# Purpose: Controls startup and scheduling of official HKO source ingestion.
# Requirement: Required; recommended value: true.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.hko-open-data] When domain_sources.hko_open_data.enabled is true: base_url must be HTTP(S); request and polling intervals must be greater than zero; daily_rainfall_lookback_days must be 1..=366; daily_temperature_lookback_months must be 1..=120.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
enabled = true
# Field: domain_sources.hko_open_data.base_url
# Purpose: HKO Open Data API root used for products that are not bound to an explicit file URL.
# Requirement: Required; recommended value: "https://data.weather.gov.hk/weatherAPI/opendata".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.hko-open-data] When domain_sources.hko_open_data.enabled is true: base_url must be HTTP(S); request and polling intervals must be greater than zero; daily_rainfall_lookback_days must be 1..=366; daily_temperature_lookback_months must be 1..=120.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
base_url = "https://data.weather.gov.hk/weatherAPI/opendata"
# Field: domain_sources.hko_open_data.request_timeout_ms
# Purpose: End-to-end timeout for one HKO HTTP request.
# Requirement: Required; recommended value: 10000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.source.hko-open-data] When domain_sources.hko_open_data.enabled is true: base_url must be HTTP(S); request and polling intervals must be greater than zero; daily_rainfall_lookback_days must be 1..=366; daily_temperature_lookback_months must be 1..=120.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
request_timeout_ms = 10000
# Field: domain_sources.hko_open_data.daily_rainfall_poll_secs
# Purpose: Poll cadence for immutable daily-rainfall products.
# Requirement: Required; recommended value: 86400.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.hko-open-data] When domain_sources.hko_open_data.enabled is true: base_url must be HTTP(S); request and polling intervals must be greater than zero; daily_rainfall_lookback_days must be 1..=366; daily_temperature_lookback_months must be 1..=120.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
daily_rainfall_poll_secs = 86400
# Field: domain_sources.hko_open_data.daily_rainfall_lookback_days
# Purpose: Completed daily-rainfall rows retained on each immutable file scan.
# Requirement: Required; recommended value: 62.
# Type/unit/range: Integer; days; minimum=0, maximum=65535.
# Validation/cross-field contract: [deploy.source.hko-open-data] When domain_sources.hko_open_data.enabled is true: base_url must be HTTP(S); request and polling intervals must be greater than zero; daily_rainfall_lookback_days must be 1..=366; daily_temperature_lookback_months must be 1..=120.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
daily_rainfall_lookback_days = 62
# Field: domain_sources.hko_open_data.daily_temperature_poll_secs
# Purpose: Poll cadence for HKO monthly files containing completed daily temperatures.
# Requirement: Required; recommended value: 1800.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.hko-open-data] When domain_sources.hko_open_data.enabled is true: base_url must be HTTP(S); request and polling intervals must be greater than zero; daily_rainfall_lookback_days must be 1..=366; daily_temperature_lookback_months must be 1..=120.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
daily_temperature_poll_secs = 1800
# Field: domain_sources.hko_open_data.daily_temperature_lookback_months
# Purpose: Maximum monthly partitions inspected newest-first when the latest documented publication is not yet present in the source file.
# Requirement: Required; recommended value: 24.
# Type/unit/range: Integer; months; minimum=0, maximum=65535.
# Validation/cross-field contract: [deploy.source.hko-open-data] When domain_sources.hko_open_data.enabled is true: base_url must be HTTP(S); request and polling intervals must be greater than zero; daily_rainfall_lookback_days must be 1..=366; daily_temperature_lookback_months must be 1..=120.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
daily_temperature_lookback_months = 24

[domain_sources.airnow]
# Field: domain_sources.airnow.enabled
# Purpose: Controls startup and scheduling of official `AirNow` ingestion.
# Requirement: Required; recommended value: true.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.airnow] When domain_sources.airnow.enabled is true: Both URLs must be HTTP(S), request_timeout_ms and poll_secs must be greater than zero, and correction_lookback_hours must be 1..=168.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
enabled = true
# Field: domain_sources.airnow.reporting_area_url
# Purpose: Official daily reporting-area file used for preliminary regional AQI.
# Requirement: Required; recommended value: "https://files.airnowtech.org/airnow/today/reportingarea.dat".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.airnow] When domain_sources.airnow.enabled is true: Both URLs must be HTTP(S), request_timeout_ms and poll_secs must be greater than zero, and correction_lookback_hours must be 1..=168.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
reporting_area_url = "https://files.airnowtech.org/airnow/today/reportingarea.dat"
# Field: domain_sources.airnow.hourly_aq_base_url
# Purpose: `AirNow` archive root used to resolve hourly site-observation files by UTC date.
# Requirement: Required; recommended value: "https://files.airnowtech.org/airnow".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.airnow] When domain_sources.airnow.enabled is true: Both URLs must be HTTP(S), request_timeout_ms and poll_secs must be greater than zero, and correction_lookback_hours must be 1..=168.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
hourly_aq_base_url = "https://files.airnowtech.org/airnow"
# Field: domain_sources.airnow.request_timeout_ms
# Purpose: End-to-end timeout for one `AirNow` HTTP request.
# Requirement: Required; recommended value: 30000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.source.airnow] When domain_sources.airnow.enabled is true: Both URLs must be HTTP(S), request_timeout_ms and poll_secs must be greater than zero, and correction_lookback_hours must be 1..=168.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
request_timeout_ms = 30000
# Field: domain_sources.airnow.poll_secs
# Purpose: Poll cadence for preliminary `AirNow` area and site observations.
# Requirement: Required; recommended value: 1800.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.airnow] When domain_sources.airnow.enabled is true: Both URLs must be HTTP(S), request_timeout_ms and poll_secs must be greater than zero, and correction_lookback_hours must be 1..=168.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
poll_secs = 1800
# Field: domain_sources.airnow.correction_lookback_hours
# Purpose: `AirNow` republishes recent hourly observations as preliminary corrections; workers must re-read at least this window.
# Requirement: Required; recommended value: 72.
# Type/unit/range: Integer; hours; minimum=0, maximum=65535.
# Validation/cross-field contract: [deploy.source.airnow] When domain_sources.airnow.enabled is true: Both URLs must be HTTP(S), request_timeout_ms and poll_secs must be greater than zero, and correction_lookback_hours must be 1..=168.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
correction_lookback_hours = 72

[domain_sources.tornado]
# Field: domain_sources.tornado.enabled
# Purpose: Controls startup and scheduling of SPC/NCEI tornado ingestion.
# Requirement: Required; recommended value: true.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.tornado] When domain_sources.tornado.enabled is true: All URLs must be HTTP(S), request/refresh/poll intervals must be greater than zero, and ncei_backfill_years must be 1..=10.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
enabled = true
# Field: domain_sources.tornado.spc_base_url
# Purpose: Official SPC preliminary severe-weather report root.
# Requirement: Required; recommended value: "https://www.spc.noaa.gov/climo/reports".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.tornado] When domain_sources.tornado.enabled is true: All URLs must be HTTP(S), request/refresh/poll intervals must be greater than zero, and ncei_backfill_years must be 1..=10.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
spc_base_url = "https://www.spc.noaa.gov/climo/reports"
# Field: domain_sources.tornado.ncei_csv_base_url
# Purpose: Official NCEI Storm Events bulk CSV archive root.
# Requirement: Required; recommended value: "https://www.ncei.noaa.gov/pub/data/swdi/stormevents/csvfiles".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.tornado] When domain_sources.tornado.enabled is true: All URLs must be HTTP(S), request/refresh/poll intervals must be greater than zero, and ncei_backfill_years must be 1..=10.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
ncei_csv_base_url = "https://www.ncei.noaa.gov/pub/data/swdi/stormevents/csvfiles"
# Field: domain_sources.tornado.ncei_time_series_base_url
# Purpose: Official NCEI tornado time-series API root used for finalized aggregates.
# Requirement: Required; recommended value: "https://www.ncei.noaa.gov/access/monitoring/tornadoes/time-series".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.tornado] When domain_sources.tornado.enabled is true: All URLs must be HTTP(S), request/refresh/poll intervals must be greater than zero, and ncei_backfill_years must be 1..=10.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
ncei_time_series_base_url = "https://www.ncei.noaa.gov/access/monitoring/tornadoes/time-series"
# Field: domain_sources.tornado.request_timeout_ms
# Purpose: End-to-end timeout for one SPC or NCEI HTTP request.
# Requirement: Required; recommended value: 120000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.source.tornado] When domain_sources.tornado.enabled is true: All URLs must be HTTP(S), request/refresh/poll intervals must be greater than zero, and ncei_backfill_years must be 1..=10.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
request_timeout_ms = 120000
# Field: domain_sources.tornado.spc_poll_secs
# Purpose: Poll cadence for intraday preliminary SPC reports.
# Requirement: Required; recommended value: 600.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.tornado] When domain_sources.tornado.enabled is true: All URLs must be HTTP(S), request/refresh/poll intervals must be greater than zero, and ncei_backfill_years must be 1..=10.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
spc_poll_secs = 600
# Field: domain_sources.tornado.ncei_refresh_secs
# Purpose: Refresh cadence for finalized NCEI Storm Events archives.
# Requirement: Required; recommended value: 2678400.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.tornado] When domain_sources.tornado.enabled is true: All URLs must be HTTP(S), request/refresh/poll intervals must be greater than zero, and ncei_backfill_years must be 1..=10.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ncei_refresh_secs = 2678400
# Field: domain_sources.tornado.ncei_time_series_poll_secs
# Purpose: Poll cadence for finalized NCEI tornado aggregate series.
# Requirement: Required; recommended value: 300.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.tornado] When domain_sources.tornado.enabled is true: All URLs must be HTTP(S), request/refresh/poll intervals must be greater than zero, and ncei_backfill_years must be 1..=10.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ncei_time_series_poll_secs = 300
# Field: domain_sources.tornado.ncei_backfill_years
# Purpose: Complete official archive years re-read on every NCEI refresh.
# Requirement: Required; recommended value: 2.
# Type/unit/range: Integer; years; minimum=0, maximum=255.
# Validation/cross-field contract: [deploy.source.tornado] When domain_sources.tornado.enabled is true: All URLs must be HTTP(S), request/refresh/poll intervals must be greater than zero, and ncei_backfill_years must be 1..=10.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ncei_backfill_years = 2

[domain_sources.nhc]
# Field: domain_sources.nhc.enabled
# Purpose: Controls startup and scheduling of official NHC ingestion.
# Requirement: Required; recommended value: true.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.nhc] When domain_sources.nhc.enabled is true: Both URLs must be HTTP(S), and request_timeout_ms, advisory_poll_secs, and best_track_refresh_secs must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
enabled = true
# Field: domain_sources.nhc.current_storms_url
# Purpose: Official NHC current-storm catalog used to discover active advisories.
# Requirement: Required; recommended value: "https://www.nhc.noaa.gov/CurrentStorms.json".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.nhc] When domain_sources.nhc.enabled is true: Both URLs must be HTTP(S), and request_timeout_ms, advisory_poll_secs, and best_track_refresh_secs must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
current_storms_url = "https://www.nhc.noaa.gov/CurrentStorms.json"
# Field: domain_sources.nhc.data_archive_url
# Purpose: Official NHC data root containing advisories and HURDAT2 best-track archives.
# Requirement: Required; recommended value: "https://www.nhc.noaa.gov/data/".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.nhc] When domain_sources.nhc.enabled is true: Both URLs must be HTTP(S), and request_timeout_ms, advisory_poll_secs, and best_track_refresh_secs must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
data_archive_url = "https://www.nhc.noaa.gov/data/"
# Field: domain_sources.nhc.request_timeout_ms
# Purpose: End-to-end timeout for one NHC HTTP request.
# Requirement: Required; recommended value: 120000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.source.nhc] When domain_sources.nhc.enabled is true: Both URLs must be HTTP(S), and request_timeout_ms, advisory_poll_secs, and best_track_refresh_secs must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
request_timeout_ms = 120000
# Field: domain_sources.nhc.advisory_poll_secs
# Purpose: Poll cadence for active-storm advisories.
# Requirement: Required; recommended value: 900.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.nhc] When domain_sources.nhc.enabled is true: Both URLs must be HTTP(S), and request_timeout_ms, advisory_poll_secs, and best_track_refresh_secs must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
advisory_poll_secs = 900
# Field: domain_sources.nhc.best_track_refresh_secs
# Purpose: Refresh cadence for historical HURDAT2 best-track archives.
# Requirement: Required; recommended value: 31536000.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.nhc] When domain_sources.nhc.enabled is true: Both URLs must be HTTP(S), and request_timeout_ms, advisory_poll_secs, and best_track_refresh_secs must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
best_track_refresh_secs = 31536000

[domain_sources.nasa_gistemp]
# Field: domain_sources.nasa_gistemp.enabled
# Purpose: Controls startup and scheduling of NASA GISTEMP ingestion.
# Requirement: Required; recommended value: true.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.nasa-gistemp] When domain_sources.nasa_gistemp.enabled is true: Both URLs must be HTTP(S), and request_timeout_ms and refresh_secs must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
enabled = true
# Field: domain_sources.nasa_gistemp.csv_url
# Purpose: Official monthly GISTEMP global anomaly CSV.
# Requirement: Required; recommended value: "https://data.giss.nasa.gov/gistemp/tabledata_v4/GLB.Ts+dSST.csv".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.nasa-gistemp] When domain_sources.nasa_gistemp.enabled is true: Both URLs must be HTTP(S), and request_timeout_ms and refresh_secs must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
csv_url = "https://data.giss.nasa.gov/gistemp/tabledata_v4/GLB.Ts+dSST.csv"
# Field: domain_sources.nasa_gistemp.annual_url
# Purpose: Official annual GISTEMP global anomaly series used for year-end resolution.
# Requirement: Required; recommended value: "https://data.giss.nasa.gov/gistemp/graphs/graph_data/Global_Mean_Estimates_based_on_Land_and_Ocean_Data/graph.txt".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.nasa-gistemp] When domain_sources.nasa_gistemp.enabled is true: Both URLs must be HTTP(S), and request_timeout_ms and refresh_secs must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
annual_url = "https://data.giss.nasa.gov/gistemp/graphs/graph_data/Global_Mean_Estimates_based_on_Land_and_Ocean_Data/graph.txt"
# Field: domain_sources.nasa_gistemp.request_timeout_ms
# Purpose: End-to-end timeout for one NASA GISTEMP HTTP request.
# Requirement: Required; recommended value: 30000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.source.nasa-gistemp] When domain_sources.nasa_gistemp.enabled is true: Both URLs must be HTTP(S), and request_timeout_ms and refresh_secs must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
request_timeout_ms = 30000
# Field: domain_sources.nasa_gistemp.refresh_secs
# Purpose: Refresh cadence for published monthly and annual GISTEMP observations.
# Requirement: Required; recommended value: 2678400.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.nasa-gistemp] When domain_sources.nasa_gistemp.enabled is true: Both URLs must be HTTP(S), and request_timeout_ms and refresh_secs must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
refresh_secs = 2678400

[domain_sources.nsidc_sea_ice]
# Field: domain_sources.nsidc_sea_ice.enabled
# Purpose: Controls startup and scheduling of official NSIDC Sea Ice Index ingestion.
# Requirement: Required; recommended value: true.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.nsidc-sea-ice] When domain_sources.nsidc_sea_ice.enabled is true: All daily and monthly URLs must be HTTP(S), and request_timeout_ms and refresh_secs must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
enabled = true
# Field: domain_sources.nsidc_sea_ice.north_daily_csv_url
# Purpose: Official Northern Hemisphere daily sea-ice extent CSV.
# Requirement: Required; recommended value: "https://noaadata.apps.nsidc.org/NOAA/G02135/north/daily/data/N_seaice_extent_daily_v4.0.csv".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.nsidc-sea-ice] When domain_sources.nsidc_sea_ice.enabled is true: All daily and monthly URLs must be HTTP(S), and request_timeout_ms and refresh_secs must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
north_daily_csv_url = "https://noaadata.apps.nsidc.org/NOAA/G02135/north/daily/data/N_seaice_extent_daily_v4.0.csv"
# Field: domain_sources.nsidc_sea_ice.south_daily_csv_url
# Purpose: Official Southern Hemisphere daily sea-ice extent CSV.
# Requirement: Required; recommended value: "https://noaadata.apps.nsidc.org/NOAA/G02135/south/daily/data/S_seaice_extent_daily_v4.0.csv".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.nsidc-sea-ice] When domain_sources.nsidc_sea_ice.enabled is true: All daily and monthly URLs must be HTTP(S), and request_timeout_ms and refresh_secs must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
south_daily_csv_url = "https://noaadata.apps.nsidc.org/NOAA/G02135/south/daily/data/S_seaice_extent_daily_v4.0.csv"
# Field: domain_sources.nsidc_sea_ice.north_monthly_base_url
# Purpose: Official Northern Hemisphere monthly sea-ice data root.
# Requirement: Required; recommended value: "https://noaadata.apps.nsidc.org/NOAA/G02135/north/monthly/data".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.nsidc-sea-ice] When domain_sources.nsidc_sea_ice.enabled is true: All daily and monthly URLs must be HTTP(S), and request_timeout_ms and refresh_secs must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
north_monthly_base_url = "https://noaadata.apps.nsidc.org/NOAA/G02135/north/monthly/data"
# Field: domain_sources.nsidc_sea_ice.south_monthly_base_url
# Purpose: Official Southern Hemisphere monthly sea-ice data root.
# Requirement: Required; recommended value: "https://noaadata.apps.nsidc.org/NOAA/G02135/south/monthly/data".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.nsidc-sea-ice] When domain_sources.nsidc_sea_ice.enabled is true: All daily and monthly URLs must be HTTP(S), and request_timeout_ms and refresh_secs must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
south_monthly_base_url = "https://noaadata.apps.nsidc.org/NOAA/G02135/south/monthly/data"
# Field: domain_sources.nsidc_sea_ice.request_timeout_ms
# Purpose: End-to-end timeout for one NSIDC HTTP request.
# Requirement: Required; recommended value: 120000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.source.nsidc-sea-ice] When domain_sources.nsidc_sea_ice.enabled is true: All daily and monthly URLs must be HTTP(S), and request_timeout_ms and refresh_secs must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
request_timeout_ms = 120000
# Field: domain_sources.nsidc_sea_ice.refresh_secs
# Purpose: Refresh cadence for daily and monthly NSIDC publications.
# Requirement: Required; recommended value: 86400.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.nsidc-sea-ice] When domain_sources.nsidc_sea_ice.enabled is true: All daily and monthly URLs must be HTTP(S), and request_timeout_ms and refresh_secs must each be greater than zero.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
refresh_secs = 86400

[domain_sources.nws_observation]
# Field: domain_sources.nws_observation.enabled
# Purpose: Controls startup and scheduling of official NWS station-observation ingestion.
# Requirement: Required; recommended value: true.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.nws-observation] When domain_sources.nws_observation.enabled is true: base_url must be HTTP(S), request_timeout_ms and poll_secs must be greater than zero, and lookback_observations must be 1..=500.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
enabled = true
# Field: domain_sources.nws_observation.base_url
# Purpose: Official api.weather.gov root used for station metadata and observations.
# Requirement: Required; recommended value: "https://api.weather.gov".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.source.nws-observation] When domain_sources.nws_observation.enabled is true: base_url must be HTTP(S), request_timeout_ms and poll_secs must be greater than zero, and lookback_observations must be 1..=500.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
base_url = "https://api.weather.gov"
# Field: domain_sources.nws_observation.request_timeout_ms
# Purpose: End-to-end timeout for one NWS HTTP request.
# Requirement: Required; recommended value: 30000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.source.nws-observation] When domain_sources.nws_observation.enabled is true: base_url must be HTTP(S), request_timeout_ms and poll_secs must be greater than zero, and lookback_observations must be 1..=500.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
request_timeout_ms = 30000
# Field: domain_sources.nws_observation.poll_secs
# Purpose: Poll cadence for the latest station observation batch.
# Requirement: Required; recommended value: 300.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.source.nws-observation] When domain_sources.nws_observation.enabled is true: base_url must be HTTP(S), request_timeout_ms and poll_secs must be greater than zero, and lookback_observations must be 1..=500.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
poll_secs = 300
# Field: domain_sources.nws_observation.lookback_observations
# Purpose: Number of newest station reports scanned to bridge normal null-value observations without inventing wind values.
# Requirement: Required; recommended value: 24.
# Type/unit/range: Integer; count; minimum=0, maximum=65535.
# Validation/cross-field contract: [deploy.source.nws-observation] When domain_sources.nws_observation.enabled is true: base_url must be HTTP(S), request_timeout_ms and poll_secs must be greater than zero, and lookback_observations must be 1..=500.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
lookback_observations = 24

[[domain_sources.weather_vertical_bindings.hko_rainfall]]
# Field: domain_sources.weather_vertical_bindings.hko_rainfall.*.site_key
# Purpose: Contract-facing HKO site label used to select this explicit binding.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "Hong Kong Observatory".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.hko-rainfall] When For every configured binding: The binding must have a unique source-native HKO station, matching official HTTP(S) daily CSV, printable site key no longer than 128 bytes, latitude in -90..=90 degrees, longitude in -180..=180 degrees, and Asia/Hong_Kong timezone.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
site_key = "Hong Kong Observatory"
# Field: domain_sources.weather_vertical_bindings.hko_rainfall.*.station_key
# Purpose: Official HKO station identifier embedded in the source product path.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "HKO".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.hko-rainfall] When For every configured binding: The binding must have a unique source-native HKO station, matching official HTTP(S) daily CSV, printable site key no longer than 128 bytes, latitude in -90..=90 degrees, longitude in -180..=180 degrees, and Asia/Hong_Kong timezone.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
station_key = "HKO"
# Field: domain_sources.weather_vertical_bindings.hko_rainfall.*.daily_csv_url
# Purpose: Exact official daily-rainfall CSV used to produce settlement evidence.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "https://data.weather.gov.hk/weatherAPI/cis/csvfile/HKO/ALL/daily_HKO_RF_ALL.csv".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.hko-rainfall] When For every configured binding: The binding must have a unique source-native HKO station, matching official HTTP(S) daily CSV, printable site key no longer than 128 bytes, latitude in -90..=90 degrees, longitude in -180..=180 degrees, and Asia/Hong_Kong timezone.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
daily_csv_url = "https://data.weather.gov.hk/weatherAPI/cis/csvfile/HKO/ALL/daily_HKO_RF_ALL.csv"
# Field: domain_sources.weather_vertical_bindings.hko_rainfall.*.latitude
# Purpose: Latitude of the exact HKO station used to validate spatial contract identity.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "22.301944".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.hko-rainfall] When For every configured binding: The binding must have a unique source-native HKO station, matching official HTTP(S) daily CSV, printable site key no longer than 128 bytes, latitude in -90..=90 degrees, longitude in -180..=180 degrees, and Asia/Hong_Kong timezone.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "22.301944"
# Field: domain_sources.weather_vertical_bindings.hko_rainfall.*.longitude
# Purpose: Longitude of the exact HKO station used to validate spatial contract identity.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "114.174167".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.hko-rainfall] When For every configured binding: The binding must have a unique source-native HKO station, matching official HTTP(S) daily CSV, printable site key no longer than 128 bytes, latitude in -90..=90 degrees, longitude in -180..=180 degrees, and Asia/Hong_Kong timezone.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "114.174167"
# Field: domain_sources.weather_vertical_bindings.hko_rainfall.*.timezone
# Purpose: IANA timezone used to close the station's observation day without UTC guessing.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "Asia/Hong_Kong".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.hko-rainfall] When For every configured binding: The binding must have a unique source-native HKO station, matching official HTTP(S) daily CSV, printable site key no longer than 128 bytes, latitude in -90..=90 degrees, longitude in -180..=180 degrees, and Asia/Hong_Kong timezone.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Hong_Kong"

[[domain_sources.weather_vertical_bindings.hko_daily_temperature]]
# Field: domain_sources.weather_vertical_bindings.hko_daily_temperature.*.station
# Purpose: Official HKO station identifier whose monthly daily-temperature file is ingested.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "HKO".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.hko-temperature] When For every configured binding: The HKO station must be source-native and unique, and timezone must equal Asia/Hong_Kong.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
station = "HKO"
# Field: domain_sources.weather_vertical_bindings.hko_daily_temperature.*.timezone
# Purpose: IANA timezone that defines the station's local observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "Asia/Hong_Kong".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.hko-temperature] When For every configured binding: The HKO station must be source-native and unique, and timezone must equal Asia/Hong_Kong.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Hong_Kong"

[[domain_sources.weather_vertical_bindings.airnow_pm25_reporting_areas]]
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_reporting_areas.*.area
# Purpose: Official `AirNow` reporting-area name matched to the market's resolution geography.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "New York City Region".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-area] When For every configured binding: Area/state identity must be unique, area must be printable and no longer than 128 bytes, state must be two uppercase ASCII letters, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
area = "New York City Region"
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_reporting_areas.*.state
# Purpose: Two-letter US state code disambiguating the official reporting-area name.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "NY".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-area] When For every configured binding: Area/state identity must be unique, area must be printable and no longer than 128 bytes, state must be two uppercase ASCII letters, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
state = "NY"
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_reporting_areas.*.timezone
# Purpose: IANA timezone used to place hourly AQI observations on the contract day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/New_York".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-area] When For every configured binding: Area/state identity must be unique, area must be printable and no longer than 128 bytes, state must be two uppercase ASCII letters, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/New_York"

[[domain_sources.weather_vertical_bindings.airnow_pm25_reporting_areas]]
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_reporting_areas.*.area
# Purpose: Official `AirNow` reporting-area name matched to the market's resolution geography.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "New York City Region".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-area] When For every configured binding: Area/state identity must be unique, area must be printable and no longer than 128 bytes, state must be two uppercase ASCII letters, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
area = "Philadelphia"
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_reporting_areas.*.state
# Purpose: Two-letter US state code disambiguating the official reporting-area name.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "NY".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-area] When For every configured binding: Area/state identity must be unique, area must be printable and no longer than 128 bytes, state must be two uppercase ASCII letters, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
state = "PA"
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_reporting_areas.*.timezone
# Purpose: IANA timezone used to place hourly AQI observations on the contract day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/New_York".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-area] When For every configured binding: Area/state identity must be unique, area must be printable and no longer than 128 bytes, state must be two uppercase ASCII letters, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/New_York"

[[domain_sources.weather_vertical_bindings.airnow_pm25_reporting_areas]]
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_reporting_areas.*.area
# Purpose: Official `AirNow` reporting-area name matched to the market's resolution geography.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "New York City Region".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-area] When For every configured binding: Area/state identity must be unique, area must be printable and no longer than 128 bytes, state must be two uppercase ASCII letters, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
area = "Columbus"
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_reporting_areas.*.state
# Purpose: Two-letter US state code disambiguating the official reporting-area name.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "NY".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-area] When For every configured binding: Area/state identity must be unique, area must be printable and no longer than 128 bytes, state must be two uppercase ASCII letters, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
state = "OH"
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_reporting_areas.*.timezone
# Purpose: IANA timezone used to place hourly AQI observations on the contract day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/New_York".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-area] When For every configured binding: Area/state identity must be unique, area must be printable and no longer than 128 bytes, state must be two uppercase ASCII letters, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/New_York"

[[domain_sources.weather_vertical_bindings.airnow_pm25_reporting_areas]]
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_reporting_areas.*.area
# Purpose: Official `AirNow` reporting-area name matched to the market's resolution geography.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "New York City Region".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-area] When For every configured binding: Area/state identity must be unique, area must be printable and no longer than 128 bytes, state must be two uppercase ASCII letters, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
area = "Chicago"
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_reporting_areas.*.state
# Purpose: Two-letter US state code disambiguating the official reporting-area name.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "NY".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-area] When For every configured binding: Area/state identity must be unique, area must be printable and no longer than 128 bytes, state must be two uppercase ASCII letters, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
state = "IL"
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_reporting_areas.*.timezone
# Purpose: IANA timezone used to place hourly AQI observations on the contract day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/New_York".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-area] When For every configured binding: Area/state identity must be unique, area must be printable and no longer than 128 bytes, state must be two uppercase ASCII letters, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/Chicago"

[[domain_sources.weather_vertical_bindings.airnow_pm25_sites]]
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_sites.*.contract_location
# Purpose: Human contract location that this exact `AirNow` monitoring site is allowed to represent.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "East Rutherford".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-site] When For every configured binding: AQSID must be a unique 12-digit identity; location/site must be printable and no longer than 128 bytes; resolution URL must be HTTP(S); state must be two uppercase ASCII letters; latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
contract_location = "East Rutherford"
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_sites.*.primary_resolution_url
# Purpose: Human-facing `AirNow` resolution page retained as primary settlement provenance.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "https://www.airnow.gov/?city=East%20Rutherford&state=NJ&country=USA".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-site] When For every configured binding: AQSID must be a unique 12-digit identity; location/site must be printable and no longer than 128 bytes; resolution URL must be HTTP(S); state must be two uppercase ASCII letters; latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
primary_resolution_url = "https://www.airnow.gov/?city=East%20Rutherford&state=NJ&country=USA"
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_sites.*.aqsid
# Purpose: Canonical Air Quality System site identifier used by `AirNow` observations.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "840340170008".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-site] When For every configured binding: AQSID must be a unique 12-digit identity; location/site must be printable and no longer than 128 bytes; resolution URL must be HTTP(S); state must be two uppercase ASCII letters; latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
aqsid = "840340170008"
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_sites.*.site_name
# Purpose: Official monitoring-site name retained in provenance and operator diagnostics.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "Union City High School".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-site] When For every configured binding: AQSID must be a unique 12-digit identity; location/site must be printable and no longer than 128 bytes; resolution URL must be HTTP(S); state must be two uppercase ASCII letters; latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
site_name = "Union City High School"
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_sites.*.state
# Purpose: Two-letter US state code used to validate the AQS site identity.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "NJ".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-site] When For every configured binding: AQSID must be a unique 12-digit identity; location/site must be printable and no longer than 128 bytes; resolution URL must be HTTP(S); state must be two uppercase ASCII letters; latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
state = "NJ"
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_sites.*.latitude
# Purpose: Latitude of the monitoring site used for exact spatial binding checks.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "40.770908".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-site] When For every configured binding: AQSID must be a unique 12-digit identity; location/site must be printable and no longer than 128 bytes; resolution URL must be HTTP(S); state must be two uppercase ASCII letters; latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "40.770908"
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_sites.*.longitude
# Purpose: Longitude of the monitoring site used for exact spatial binding checks.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-74.036218".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-site] When For every configured binding: AQSID must be a unique 12-digit identity; location/site must be printable and no longer than 128 bytes; resolution URL must be HTTP(S); state must be two uppercase ASCII letters; latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "-74.036218"
# Field: domain_sources.weather_vertical_bindings.airnow_pm25_sites.*.timezone
# Purpose: IANA timezone used to construct the site's contract-day AQI maximum.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/New_York".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.airnow-site] When For every configured binding: AQSID must be a unique 12-digit identity; location/site must be printable and no longer than 128 bytes; resolution URL must be HTTP(S); state must be two uppercase ASCII letters; latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/New_York"

[[domain_sources.weather_vertical_bindings.tornado_regions]]
# Field: domain_sources.weather_vertical_bindings.tornado_regions.*.region_id
# Purpose: Stable deployment-owned identifier used by market linkage and evidence lineage.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "united_states".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.tornado-region] When For every configured binding: Region identity must be unique and printable within 64 bytes; scope must be United States or a valid state code/name pair; timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
region_id = "united_states"
# Field: domain_sources.weather_vertical_bindings.tornado_regions.*.timezone
# Purpose: IANA timezone used to delimit the region's contract day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/New_York".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.tornado-region] When For every configured binding: Region identity must be unique and printable within 64 bytes; scope must be United States or a valid state code/name pair; timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/New_York"

# Field: domain_sources.weather_vertical_bindings.tornado_regions.*.scope
# Purpose: Exact national or state scope applied to SPC and NCEI tornado records.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: {"kind":"state","ncei_state_name":"OKLAHOMA","spc_state_code":"OK"}.
# Type/unit/range: Variant; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.tornado-region] When For every configured binding: Region identity must be unique and printable within 64 bytes; scope must be United States or a valid state code/name pair; timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: state, united_states.
[domain_sources.weather_vertical_bindings.tornado_regions.scope]
kind = "united_states"

[[domain_sources.weather_vertical_bindings.tornado_regions]]
# Field: domain_sources.weather_vertical_bindings.tornado_regions.*.region_id
# Purpose: Stable deployment-owned identifier used by market linkage and evidence lineage.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "united_states".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.tornado-region] When For every configured binding: Region identity must be unique and printable within 64 bytes; scope must be United States or a valid state code/name pair; timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
region_id = "oklahoma"
# Field: domain_sources.weather_vertical_bindings.tornado_regions.*.timezone
# Purpose: IANA timezone used to delimit the region's contract day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/New_York".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.tornado-region] When For every configured binding: Region identity must be unique and printable within 64 bytes; scope must be United States or a valid state code/name pair; timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/Chicago"

# Field: domain_sources.weather_vertical_bindings.tornado_regions.*.scope
# Purpose: Exact national or state scope applied to SPC and NCEI tornado records.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: {"kind":"state","ncei_state_name":"OKLAHOMA","spc_state_code":"OK"}.
# Type/unit/range: Variant; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.tornado-region] When For every configured binding: Region identity must be unique and printable within 64 bytes; scope must be United States or a valid state code/name pair; timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: state, united_states.
[domain_sources.weather_vertical_bindings.tornado_regions.scope]
kind = "state"
spc_state_code = "OK"
ncei_state_name = "OKLAHOMA"

[[domain_sources.weather_vertical_bindings.nhc_historical_storms]]
# Field: domain_sources.weather_vertical_bindings.nhc_historical_storms.*.basin
# Purpose: NHC ocean-basin catalog whose storm identifiers are valid for this binding.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "atlantic".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.nhc-storm] When For every configured binding: Basin and eight-character storm ID must form a unique supported HURDAT2 identity with the correct AL, EP, or CP prefix.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
basin = "atlantic"
# Field: domain_sources.weather_vertical_bindings.nhc_historical_storms.*.storm_id
# Purpose: Canonical NHC storm identifier whose immutable HURDAT2 track is consumed.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "AL092021".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.nhc-storm] When For every configured binding: Basin and eight-character storm ID must form a unique supported HURDAT2 identity with the correct AL, EP, or CP prefix.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
storm_id = "AL092021"

[[domain_sources.weather_vertical_bindings.nws_wind_stations]]
# Field: domain_sources.weather_vertical_bindings.nws_wind_stations.*.station
# Purpose: ICAO/NWS station identifier whose official observations determine wind extremes.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "KMWN".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.nws-wind] When For every configured binding: Station must be a unique ICAO identifier and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
station = "KMWN"
# Field: domain_sources.weather_vertical_bindings.nws_wind_stations.*.timezone
# Purpose: IANA timezone used to delimit the station's contract day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/New_York".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-vertical.nws-wind] When For every configured binding: Station must be a unique ICAO identifier and timezone must be an IANA identifier.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/New_York"

[domain_sources.weather_stations.CYYZ]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/Toronto"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "43.679"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "-79.629"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "171"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "CAN06158731"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "CAN06158731"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.EDDM]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Europe/Berlin"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "48.348"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "11.813"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "445"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "GMI0000EDDM"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "GMI0000EDDM"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.EFHK]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Europe/Helsinki"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "60.327"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "24.957"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "56"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "FIMU0002974"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "FIMU0002974"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.EGLC]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Europe/London"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "51.505"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "0.055"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "10"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "UKI0000EGLC"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "UKI0000EGLC"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.EHAM]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Europe/Amsterdam"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "52.315"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "4.790"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "-2"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "NLMU0029295"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "NLMU0029295"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.EPWA]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Europe/Warsaw"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "52.163"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "20.961"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "107"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "PLI0000EPBC"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "official_nearby_proxy"

[domain_sources.weather_stations.FACT]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Africa/Johannesburg"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "-33.965"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "18.602"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "48"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "SFM00068999"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "SFM00068999"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.KATL]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/New_York"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "33.62972"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "-84.44223"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "309"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "USW00013874"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "USW00013874"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.KAUS]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/Chicago"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "30.1831"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "-97.68063"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "148"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "USW00013904"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "USW00013904"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.KBKF]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/Denver"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "39.713"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "-104.758"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "1703"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "USW00023036"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "USW00023036"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.KDAL]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/Chicago"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "32.83836"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "-96.83584"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "148"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "USW00013960"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "USW00013960"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.KHOU]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/Chicago"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "29.64582"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "-95.28214"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "13"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "USW00012918"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "USW00012918"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.KLAX]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/Los_Angeles"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "33.93817"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "-118.3866"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "30"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "USW00023174"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "USW00023174"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.KLGA]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/New_York"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "40.77945"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "-73.88027"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "9"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "USW00014732"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "USW00014732"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.KMIA]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/New_York"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "25.78806"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "-80.31692"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "1"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "USW00012839"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "USW00012839"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.KORD]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/Chicago"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "41.96017"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "-87.93161"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "202"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "USW00094846"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "USW00094846"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.KSEA]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/Los_Angeles"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "47.44467"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "-122.31442"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "115"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "USW00024233"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "USW00024233"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.KSFO]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/Los_Angeles"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "37.61961"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "-122.36561"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "2"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "USW00023234"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "USW00023234"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.LEMD]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Europe/Madrid"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "40.466"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "-3.555"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "589"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "SPMU0098221"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "SPMU0098221"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.LFPB]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Europe/Paris"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "48.967"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "2.428"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "50"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "FRI0000LFPG"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "official_nearby_proxy"

[domain_sources.weather_stations.LIMC]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Europe/Rome"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "45.631"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "8.728"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "221"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "ITMU0016064"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "official_nearby_proxy"

[domain_sources.weather_stations.LLBG]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Jerusalem"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "32.011"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "34.887"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "35"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "ISM00040179"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "official_nearby_proxy"

[domain_sources.weather_stations.LTAC]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Europe/Istanbul"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "40.128"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "32.995"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "952"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "TUM00017130"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "official_nearby_proxy"

[domain_sources.weather_stations.LTFM]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Europe/Istanbul"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "41.262"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "28.740"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "99"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "TUI0000LTFM"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "TUI0000LTFM"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.MMMX]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/Mexico_City"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "19.436"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "-99.072"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "2224"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "MXI0000MMMX"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "MXI0000MMMX"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.MPMG]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/Panama"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "8.967"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "-79.555"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "6"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "PMW00010718"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "official_nearby_proxy"

[domain_sources.weather_stations.NZWN]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Pacific/Auckland"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "-41.331"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "174.806"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "12"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "NZI0000NZWN"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "NZI0000NZWN"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.OEJN]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Riyadh"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "21.685"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "39.166"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "8"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "SAI0000OEJN"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "SAI0000OEJN"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.OPKC]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Karachi"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "24.902"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "67.139"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "20"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "unavailable"

[domain_sources.weather_stations.RCSS]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Taipei"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "25.069"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "121.552"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "8"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "unavailable"

[domain_sources.weather_stations.RJTT]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Tokyo"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "35.553"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "139.781"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "5"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "JAI0000RJTT"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "JAI0000RJTT"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.RKPK]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Seoul"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "35.179"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "128.938"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "3"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "KSI0000RKPK"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "KSI0000RKPK"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.RKSI]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Seoul"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "37.469"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "126.451"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "7"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "KSI0000RKSI"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "KSI0000RKSI"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.RPLL]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Manila"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "14.507"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "121.004"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "15"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "RPI0000RPLL"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "RPI0000RPLL"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.SAEZ]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/Argentina/Buenos_Aires"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "-34.822"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "-58.536"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "16"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "ARI0000SAEZ"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "ARI0000SAEZ"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.SBGR]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "America/Sao_Paulo"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "-23.432"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "-46.469"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "745"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "BRI0000SBGR"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "BRI0000SBGR"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.UUWW]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Europe/Moscow"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "55.592"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "37.261"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "195"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "RSI0000UUDD"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "official_nearby_proxy"

[domain_sources.weather_stations.VILK]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Kolkata"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "26.761"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "80.889"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "121"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "INI0000VILK"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "INI0000VILK"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.WMKK]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Kuala_Lumpur"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "2.747"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "101.714"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "21"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "MYI0000WMKK"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "MYI0000WMKK"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.WSSS]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Singapore"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "1.368"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "103.982"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "17"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "SNI0000WSSS"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "SNI0000WSSS"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.ZBAA]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Shanghai"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "40.082"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "116.603"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "31"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "unavailable"

[domain_sources.weather_stations.ZGGG]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Shanghai"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "23.392"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "113.307"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "11"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "CHI0000ZGGG"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "CHI0000ZGGG"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.ZGSZ]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Shanghai"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "22.639"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "113.803"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "18"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "CHI0000ZGSZ"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "CHI0000ZGSZ"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.ZHHH]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Shanghai"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "30.783"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "114.205"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "33"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "unavailable"

[domain_sources.weather_stations.ZSPD]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Shanghai"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "31.146"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "121.800"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "4"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "CHI0000ZSPD"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "CHI0000ZSPD"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.ZSQD]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Shanghai"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "36.362"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "120.087"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "2"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "unavailable"

[domain_sources.weather_stations.ZUCK]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Shanghai"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "29.718"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "106.639"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "416"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "CHI0000ZUCK"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "CHI0000ZUCK"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[domain_sources.weather_stations.ZUUU]
# Field: domain_sources.weather_stations.*.timezone
# Purpose: IANA timezone that defines the station's contract observation day.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "America/Toronto".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timezone = "Asia/Shanghai"
# Field: domain_sources.weather_stations.*.latitude
# Purpose: Latitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "43.679".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
latitude = "30.576"
# Field: domain_sources.weather_stations.*.longitude
# Purpose: Longitude of the contract station used for spatial identity and weather-model inputs.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "-79.629".
# Type/unit/range: Decimal; degrees; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
longitude = "103.950"
# Field: domain_sources.weather_stations.*.elevation_meters
# Purpose: Station elevation above mean sea level used by elevation-sensitive weather features.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "171".
# Type/unit/range: Decimal; meters; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
elevation_meters = "494"
# Field: domain_sources.weather_stations.*.ghcnh_station_id
# Purpose: Optional exact `GHCNh` station identifier used for hourly calibration history.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnh_station_id = "CHI0000ZUUU"
# Field: domain_sources.weather_stations.*.ghcnd_station_id
# Purpose: Optional exact `GHCNd` station identifier used for archive-quality daily truth.
# Requirement: Optional; recommended value: "CAN06158731".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
ghcnd_station_id = "CHI0000ZUUU"
# Field: domain_sources.weather_stations.*.historical_binding_kind
# Purpose: Declares whether historical calibration is exact, an explicit official proxy, or unavailable.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: "exact_station".
# Type/unit/range: String; unitless; enum=exact_station|official_nearby_proxy|unavailable.
# Validation/cross-field contract: [deploy.weather-station.binding] When For every configured weather-station map entry: The map key must be a valid ICAO station, timezone must be an IANA identifier, latitude must be -90..=90 degrees, longitude must be -180..=180 degrees, and GHCNh/GHCNd identities must match historical_binding_kind.
# Operational impact: Changes quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api typed external-domain adapter and quant-pivot-research PIT feature builder
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
historical_binding_kind = "exact_station"

[observability]
# Field: observability.log_level
# Purpose: Default `tracing` filter directive (e.g. `info`, `info,quant_pivot_core=debug`). The `RUST_LOG` environment variable, when set, overrides this value entirely. Default: `info`.
# Requirement: Required; recommended value: "info".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-bin tracing and metrics bootstrap; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-bin tracing and metrics bootstrap
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
log_level = "info"
# Field: observability.log_json
# Purpose: Emit JSON-structured log lines instead of human-readable text. Enable in production for log aggregation. Default: `false`.
# Requirement: Required; recommended value: false.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-bin tracing and metrics bootstrap; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-bin tracing and metrics bootstrap
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
log_json = true

[notifications.telegram]
# Field: notifications.telegram.bot_token
# Purpose: Zeroizing Telegram bot token used only by the operator-notification adapter.
# Requirement: Required; recommended value: "".
# Type/unit/range: String; unitless; write_only.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core Telegram operator notification adapter; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core Telegram operator notification adapter
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: secret; deployment projection never exposes protected literals.
# Variants: not applicable.
bot_token = ""
# Field: notifications.telegram.chat_id
# Purpose: Telegram chat identifier; this is a destination binding, not a secret.
# Requirement: Required; recommended value: "".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core Telegram operator notification adapter; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core Telegram operator notification adapter
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_identifier; deployment projection never exposes protected literals.
# Variants: not applicable.
chat_id = ""

[notifications.webhook]
# Field: notifications.webhook.url
# Purpose: HTTPS endpoint. Webhook paths commonly carry bearer material, so the complete endpoint is treated as a deploy secret even when a separate authorization header is configured.
# Requirement: Required; recommended value: "".
# Type/unit/range: String; unitless; write_only.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core authenticated operator webhook adapter; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core authenticated operator webhook adapter
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: secret; deployment projection never exposes protected literals.
# Variants: not applicable.
url = ""
# Field: notifications.webhook.authorization
# Purpose: Optional complete HTTP Authorization value.
# Requirement: Required; recommended value: "".
# Type/unit/range: String; unitless; write_only.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core authenticated operator webhook adapter; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core authenticated operator webhook adapter
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: secret; deployment projection never exposes protected literals.
# Variants: not applicable.
authorization = ""

[db.postgres]
# Field: db.postgres.host
# Purpose: Server host. Default: `localhost`.
# Requirement: Required; recommended value: "localhost".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
host = "postgres.internal.example.invalid"
# Field: db.postgres.port
# Purpose: `PostgreSQL` TCP port used by every transactional repository pool. Default: `5432`.
# Requirement: Required; recommended value: 5432.
# Type/unit/range: Integer; port; minimum=0, maximum=65535.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
port = 5432
# Field: db.postgres.user
# Purpose: Database role used by both runtime and explicit schema commands. Default: `quant_pivot`.
# Requirement: Required; recommended value: "quant_pivot".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.postgres.user] When Always: The PostgreSQL user must contain at least one non-whitespace character.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_identifier; deployment projection never exposes protected literals.
# Variants: not applicable.
user = "quant_pivot_runtime"
# Field: db.postgres.password
# Purpose: Zeroizing `PostgreSQL` authentication secret; safe projections expose only configured state.
# Requirement: Required; recommended value: "".
# Type/unit/range: String; unitless; write_only.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: secret; deployment projection never exposes protected literals.
# Variants: not applicable.
password = "REPLACE_WITH_DB_POSTGRES_PASSWORD"
# Field: db.postgres.database
# Purpose: Database name. Default: `quant_pivot`.
# Requirement: Required; recommended value: "quant_pivot".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
database = "quant_pivot"
# Field: db.postgres.schema
# Purpose: Schema search path. Default: `public`.
# Requirement: Required; recommended value: "public".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
schema = "public"
# Field: db.postgres.max_connections
# Purpose: Pool upper bound. Size for worst-case concurrent repository access. Default: `10`.
# Requirement: Required; recommended value: 10.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.postgres.pool] When Always: max_connections must be greater than zero and no less than min_connections.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_connections = 10
# Field: db.postgres.min_connections
# Purpose: Minimum number of `PostgreSQL` connections kept warm by the process pool. Default: `2`.
# Requirement: Required; recommended value: 2.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.postgres.pool] When Always: max_connections must be greater than zero and no less than min_connections.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
min_connections = 2
# Field: db.postgres.connect_timeout_secs
# Purpose: TCP connect timeout (seconds). Default: `10`.
# Requirement: Required; recommended value: 10.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
connect_timeout_secs = 10
# Field: db.postgres.idle_timeout_secs
# Purpose: Idle connection reap timeout (seconds). Default: `300`.
# Requirement: Required; recommended value: 300.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
idle_timeout_secs = 300
# Field: db.postgres.acquire_timeout_secs
# Purpose: Pool acquire timeout (seconds); exceeding indicates pool exhaustion. Default: `10`.
# Requirement: Required; recommended value: 10.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
acquire_timeout_secs = 10
# Field: db.postgres.max_lifetime_secs
# Purpose: Max connection lifetime (seconds) before recycling. Default: `1800`.
# Requirement: Required; recommended value: 1800.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_lifetime_secs = 1800
# Field: db.postgres.statement_timeout_ms
# Purpose: `statement_timeout` prevents runaway queries. Applied per-connection via libpq startup `options` so every pool connection inherits it. Unit: milliseconds, `0` = disabled. Default: `30000`.
# Requirement: Required; recommended value: 30000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
statement_timeout_ms = 30000
# Field: db.postgres.idle_in_transaction_timeout_ms
# Purpose: `idle_in_transaction_session_timeout` kills idle transactions that hold locks. Unit: milliseconds, `0` = disabled. Default: `60000`.
# Requirement: Required; recommended value: 60000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
idle_in_transaction_timeout_ms = 60000
# Field: db.postgres.lock_timeout_ms
# Purpose: `lock_timeout` kills statements that wait too long for a row/table lock. Unit: milliseconds, `0` = disabled. Default: `5000`.
# Requirement: Required; recommended value: 5000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
lock_timeout_ms = 5000
# Field: db.postgres.work_mem
# Purpose: Per-operation sort/hash memory, passed verbatim to `PostgreSQL`. Default: `16MB`.
# Requirement: Required; recommended value: "16MB".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
work_mem = "16MB"
# Field: db.postgres.verify_session_params
# Purpose: Post-connect self-check that session GUC parameters actually took effect (catches `PgBouncer` stripping startup `options`). On mismatch: abort startup with a descriptive error. Default: `true`.
# Requirement: Required; recommended value: true.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
verify_session_params = true
# Field: db.postgres.statement_cache_capacity
# Purpose: Prepared-statement cache capacity per connection. Default: `256`.
# Requirement: Required; recommended value: 256.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
statement_cache_capacity = 256
# Field: db.postgres.application_name
# Purpose: Application name reported to `pg_stat_activity`. Default: `quant-pivot`.
# Requirement: Required; recommended value: "quant-pivot".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage PostgreSQL transaction and repository pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage PostgreSQL transaction and repository pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
application_name = "quant-pivot"

[db.clickhouse]
# Field: db.clickhouse.deployment_id
# Purpose: Stable deployment identity included in signed research evidence.
# Requirement: Required; recommended value: "local-development".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage ClickHouse fact, lineage, and analytics pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage ClickHouse fact, lineage, and analytics pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_identifier; deployment projection never exposes protected literals.
# Variants: not applicable.
deployment_id = "production-primary"
# Field: db.clickhouse.cluster_id
# Purpose: `ClickHouse` Cloud cluster/service identity, or an explicit local identity.
# Requirement: Required; recommended value: "local-clickhouse".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage ClickHouse fact, lineage, and analytics pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage ClickHouse fact, lineage, and analytics pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_identifier; deployment projection never exposes protected literals.
# Variants: not applicable.
cluster_id = "clickhouse-service-id"
# Field: db.clickhouse.url
# Purpose: HTTP endpoint. Default: `http://localhost:8123`.
# Requirement: Required; recommended value: "http://localhost:8123".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage ClickHouse fact, lineage, and analytics pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage ClickHouse fact, lineage, and analytics pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
url = "https://clickhouse.internal.example.invalid:8443"
# Field: db.clickhouse.database
# Purpose: Database name. Default: `quant_pivot`.
# Requirement: Required; recommended value: "quant_pivot".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage ClickHouse fact, lineage, and analytics pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage ClickHouse fact, lineage, and analytics pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
database = "quant_pivot"
# Field: db.clickhouse.user
# Purpose: `ClickHouse` service identity authenticated by ingestion and analytical query clients. Default: `default`.
# Requirement: Required; recommended value: "default".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.clickhouse.user] When Always: The ClickHouse user must contain at least one non-whitespace character.
# Operational impact: Changes quant-pivot-storage ClickHouse fact, lineage, and analytics pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage ClickHouse fact, lineage, and analytics pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_identifier; deployment projection never exposes protected literals.
# Variants: not applicable.
user = "quant_pivot_runtime"
# Field: db.clickhouse.password
# Purpose: Zeroizing `ClickHouse` authentication secret; safe projections expose only configured state.
# Requirement: Required; recommended value: "".
# Type/unit/range: String; unitless; write_only.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage ClickHouse fact, lineage, and analytics pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage ClickHouse fact, lineage, and analytics pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: secret; deployment projection never exposes protected literals.
# Variants: not applicable.
password = "REPLACE_WITH_DB_CLICKHOUSE_PASSWORD"
# Field: db.clickhouse.flush_interval_secs
# Purpose: Max age (seconds) of a partial batch before it is flushed. Lower = fresher analytics, more insert requests. Default: `5`.
# Requirement: Required; recommended value: 5.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.clickhouse.writer] When Always: The batch size, flush interval, and concurrent insert limit must each be greater than zero.
# Operational impact: Changes quant-pivot-storage ClickHouse fact, lineage, and analytics pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage ClickHouse fact, lineage, and analytics pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
flush_interval_secs = 5
# Field: db.clickhouse.batch_size
# Purpose: Rows per insert batch; a full batch flushes immediately. `ClickHouse` favors large batches — sized for the L2 tick feed (~3K rows/s peaks). Default: `5000`.
# Requirement: Required; recommended value: 5000.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.clickhouse.writer] When Always: The batch size, flush interval, and concurrent insert limit must each be greater than zero.
# Operational impact: Changes quant-pivot-storage ClickHouse fact, lineage, and analytics pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage ClickHouse fact, lineage, and analytics pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
batch_size = 5000
# Field: db.clickhouse.max_concurrent_inserts
# Purpose: Maximum concurrent insert operations (semaphore). Prevents overwhelming the server under high tick ingestion rates. Default: `8`.
# Requirement: Required; recommended value: 8.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.clickhouse.writer] When Always: The batch size, flush interval, and concurrent insert limit must each be greater than zero.
# Operational impact: Changes quant-pivot-storage ClickHouse fact, lineage, and analytics pools; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage ClickHouse fact, lineage, and analytics pools
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_concurrent_inserts = 8

[cache]
# Field: cache.operation_timeout_ms
# Purpose: Global operation timeout (ms). Per-domain overrides take precedence. Default: `500`.
# Requirement: Required; recommended value: 500.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage bounded cache facade and domain cache policies; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage bounded cache facade and domain cache policies
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
operation_timeout_ms = 500
# Field: cache.fail_open
# Purpose: Whether cache failures are transparent to callers (`true` = never propagate errors; callers fall through to the source of truth). Default: `true`.
# Requirement: Required; recommended value: true.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage bounded cache facade and domain cache policies; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage bounded cache facade and domain cache policies
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
fail_open = true
# Field: cache.disabled
# Purpose: Disable the entire cache layer (all operations become no-ops). Default: `false`.
# Requirement: Required; recommended value: false.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage bounded cache facade and domain cache policies; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage bounded cache facade and domain cache policies
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
disabled = false

[cache.redis]
# Field: cache.redis.host
# Purpose: Server host. Default: `localhost`.
# Requirement: Required; recommended value: "localhost".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.redis.connection] When Always: host must be non-empty, port must be greater than zero, and host/port/user/password/database must compose a valid Redis connection URL.
# Operational impact: Changes quant-pivot-storage Redis cache and quant-pivot-web revocation store; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage Redis cache and quant-pivot-web revocation store
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
host = "redis.internal.example.invalid"
# Field: cache.redis.port
# Purpose: Redis TCP port used by cache, revocation, and coordination clients. Default: `6379`.
# Requirement: Required; recommended value: 6379.
# Type/unit/range: Integer; port; minimum=0, maximum=65535.
# Validation/cross-field contract: [deploy.redis.connection] When Always: host must be non-empty, port must be greater than zero, and host/port/user/password/database must compose a valid Redis connection URL.
# Operational impact: Changes quant-pivot-storage Redis cache and quant-pivot-web revocation store; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage Redis cache and quant-pivot-web revocation store
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
port = 6379
# Field: cache.redis.user
# Purpose: ACL username. Leave empty when the server uses password-only auth (`requirepass`). Default: empty.
# Requirement: Required; recommended value: "".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.redis.connection] When Always: host must be non-empty, port must be greater than zero, and host/port/user/password/database must compose a valid Redis connection URL.
# Operational impact: Changes quant-pivot-storage Redis cache and quant-pivot-web revocation store; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage Redis cache and quant-pivot-web revocation store
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_identifier; deployment projection never exposes protected literals.
# Variants: not applicable.
user = "quant_pivot_runtime"
# Field: cache.redis.password
# Purpose: Redis authentication secret source.
# Requirement: Required; recommended value: "".
# Type/unit/range: String; unitless; write_only.
# Validation/cross-field contract: [deploy.redis.connection] When Always: host must be non-empty, port must be greater than zero, and host/port/user/password/database must compose a valid Redis connection URL.
# Operational impact: Changes quant-pivot-storage Redis cache and quant-pivot-web revocation store; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage Redis cache and quant-pivot-web revocation store
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: secret; deployment projection never exposes protected literals.
# Variants: not applicable.
password = "REPLACE_WITH_CACHE_REDIS_PASSWORD"
# Field: cache.redis.database
# Purpose: Logical database index (`SELECT`). Default: `0`.
# Requirement: Required; recommended value: 0.
# Type/unit/range: Integer; index; minimum=0, maximum=255.
# Validation/cross-field contract: [deploy.redis.connection] When Always: host must be non-empty, port must be greater than zero, and host/port/user/password/database must compose a valid Redis connection URL.
# Operational impact: Changes quant-pivot-storage Redis cache and quant-pivot-web revocation store; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage Redis cache and quant-pivot-web revocation store
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
database = 0
# Field: cache.redis.pool_size
# Purpose: Connection pool size. Default: `8`.
# Requirement: Required; recommended value: 8.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.redis.pool] When Always: The Redis pool size must be greater than zero.
# Operational impact: Changes quant-pivot-storage Redis cache and quant-pivot-web revocation store; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage Redis cache and quant-pivot-web revocation store
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
pool_size = 8
# Field: cache.redis.timeout_ms
# Purpose: Per-operation timeout (ms): how long a caller waits for a pooled connection at steady state. Default: `1000`.
# Requirement: Required; recommended value: 1000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage Redis cache and quant-pivot-web revocation store; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage Redis cache and quant-pivot-web revocation store
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timeout_ms = 1000
# Field: cache.redis.connect_timeout_ms
# Purpose: Startup readiness budget (ms): how long pool creation may take to establish and PING the first connection before the process fails closed. Kept separate from [`timeout_ms`](Self::timeout_ms) so a tight per-operation deadline never starves initial connection establishment under load. Default: `5000`.
# Requirement: Required; recommended value: 5000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage Redis cache and quant-pivot-web revocation store; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage Redis cache and quant-pivot-web revocation store
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
connect_timeout_ms = 5000
# Field: cache.redis.key_prefix
# Purpose: Key namespace prefix applied to every platform Redis key (cache keys directly; revoked JWT ids under `{key_prefix}jwt:blacklist:`). Default: `qp:`.
# Requirement: Required; recommended value: "qp:".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage Redis cache and quant-pivot-web revocation store; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage Redis cache and quant-pivot-web revocation store
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
key_prefix = "qp:"

[cache.moka]
# Field: cache.moka.max_capacity
# Purpose: Maximum number of cached entries. Default: `10000`.
# Requirement: Required; recommended value: 10000.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage bounded cache facade and domain cache policies; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage bounded cache facade and domain cache policies
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_capacity = 10000

[cache.domains.market]
# Field: cache.domains.*.timeout_ms
# Purpose: Override operation timeout for this domain (ms).
# Requirement: Optional; recommended value: no implicit default.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage bounded cache facade and domain cache policies; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage bounded cache facade and domain cache policies
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
timeout_ms = 500
# Field: cache.domains.*.fail_open
# Purpose: Override fail-open for this domain.
# Requirement: Optional; recommended value: no implicit default.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage bounded cache facade and domain cache policies; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage bounded cache facade and domain cache policies
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
fail_open = true
# Field: cache.domains.*.disabled
# Purpose: Disable caching for this domain entirely.
# Requirement: Required within each configured dynamic binding; the binding map may be empty; recommended value: no implicit default.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-storage bounded cache facade and domain cache policies; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-storage bounded cache facade and domain cache policies
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
disabled = false

[keys]
# Field: keys.private_key
# Purpose: Wallet private key for signing, CLOB L1 auth, and runtime L2 derivation.
# Requirement: Optional; recommended value: no implicit default.
# Type/unit/range: String; unitless; write_only.
# Validation/cross-field contract: [deploy.account-read-credentials] When Whenever the production account is read: A private key and non-empty funder address are required because reports freeze real collateral and position state; account reads are not simulation.
# Operational impact: Changes quant-pivot-api keystore and venue identity bootstrap; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-api keystore and venue identity bootstrap
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: secret; deployment projection never exposes protected literals.
# Variants: not applicable.
private_key = "REPLACE_WITH_KEYS_PRIVATE_KEY"

[web]
# Field: web.listen_host
# Purpose: Bind address (default `0.0.0.0`).
# Requirement: Required; recommended value: "0.0.0.0".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-web HTTP, WebSocket, authorization, and static UI server; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-web HTTP, WebSocket, authorization, and static UI server
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
listen_host = "0.0.0.0"
# Field: web.listen_port
# Purpose: Bind port (default `8080`). Also serves Prometheus `GET /metrics`.
# Requirement: Required; recommended value: 8080.
# Type/unit/range: Integer; port; minimum=0, maximum=65535.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-web HTTP, WebSocket, authorization, and static UI server; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-web HTTP, WebSocket, authorization, and static UI server
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
listen_port = 8080
# Field: web.cors_allowed_origins
# Purpose: Allowed CORS origins; empty disables cross-origin requests.
# Requirement: Required; recommended value: no implicit default.
# Type/unit/range: StringArray; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-web HTTP, WebSocket, authorization, and static UI server; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-web HTTP, WebSocket, authorization, and static UI server
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_identifier; deployment projection never exposes protected literals.
# Variants: not applicable.
cors_allowed_origins = []
# Field: web.serve_static_ui
# Purpose: Whether to serve the bundled SPA from [`Self::static_ui_dir`].
# Requirement: Required; recommended value: false.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-web HTTP, WebSocket, authorization, and static UI server; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-web HTTP, WebSocket, authorization, and static UI server
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
serve_static_ui = true
# Field: web.static_ui_dir
# Purpose: Directory of the built SPA assets (default `static/ui`).
# Requirement: Required; recommended value: "static/ui".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-web HTTP, WebSocket, authorization, and static UI server; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-web HTTP, WebSocket, authorization, and static UI server
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
static_ui_dir = "static/ui"

[web.password_crypto]
# Field: web.password_crypto.max_in_flight
# Purpose: Maximum admitted operations, including the one executing on the isolated security pool. Excess requests fail fast with HTTP 429.
# Requirement: Required; recommended value: 8.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.web.password-crypto-concurrency] When Always: Password-crypto concurrency must be between 1 and 64 inclusive.
# Operational impact: Changes quant-pivot-web authentication, token verification, and password-crypto boundary; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-web authentication, token verification, and password-crypto boundary
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_in_flight = 8
# Field: web.password_crypto.deadline_ms
# Purpose: End-to-end queue plus computation deadline.
# Requirement: Required; recommended value: 15000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.web.password-crypto-deadline] When Always: The password-crypto deadline must be between 1000 and 60000 ms inclusive.
# Operational impact: Changes quant-pivot-web authentication, token verification, and password-crypto boundary; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-web authentication, token verification, and password-crypto boundary
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
deadline_ms = 15000

[web.jwt]
# Field: web.jwt.signing_key
# Purpose: Base64URL-no-pad 32-byte HS256 signing-key source.
# Requirement: Required; recommended value: "".
# Type/unit/range: String; unitless; write_only.
# Validation/cross-field contract: [deploy.web.jwt-signing-key] When Every runtime mode: The value must be a Base64URL-no-pad encoded 32-byte HS256 signing key. [deploy.evidence.key-separation] When When the evidence-attestation signing key is configured: The evidence-attestation signing key must be cryptographically independent from the JWT signing key.
# Operational impact: Changes quant-pivot-web authentication, token verification, and password-crypto boundary; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-web authentication, token verification, and password-crypto boundary
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: secret; deployment projection never exposes protected literals.
# Variants: not applicable.
signing_key = "REPLACE_WITH_WEB_JWT_SIGNING_KEY"
# Field: web.jwt.issuer
# Purpose: Token issuer claim (default `quant-pivot`).
# Requirement: Required; recommended value: "quant-pivot".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.web.jwt-identity] When Always: Issuer and audience must each contain at least one non-whitespace character.
# Operational impact: Changes quant-pivot-web authentication, token verification, and password-crypto boundary; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-web authentication, token verification, and password-crypto boundary
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_identifier; deployment projection never exposes protected literals.
# Variants: not applicable.
issuer = "quant-pivot"
# Field: web.jwt.audience
# Purpose: Token audience claim (default `quant-pivot-web`).
# Requirement: Required; recommended value: "quant-pivot-web".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.web.jwt-identity] When Always: Issuer and audience must each contain at least one non-whitespace character.
# Operational impact: Changes quant-pivot-web authentication, token verification, and password-crypto boundary; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-web authentication, token verification, and password-crypto boundary
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_identifier; deployment projection never exposes protected literals.
# Variants: not applicable.
audience = "quant-pivot-web"
# Field: web.jwt.access_ttl_secs
# Purpose: Access-token lifetime in seconds (default 900 = 15m).
# Requirement: Required; recommended value: 900.
# Type/unit/range: Integer; seconds; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.web.jwt-ttl] When Always: All token TTLs must be positive; access_ttl_secs and refresh_ttl_secs must each be no greater than absolute_session_ttl_secs.
# Operational impact: Changes quant-pivot-web authentication, token verification, and password-crypto boundary; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-web authentication, token verification, and password-crypto boundary
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
access_ttl_secs = 900
# Field: web.jwt.refresh_ttl_secs
# Purpose: Refresh-token lifetime in seconds (default 604800 = 7d).
# Requirement: Required; recommended value: 604800.
# Type/unit/range: Integer; seconds; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.web.jwt-ttl] When Always: All token TTLs must be positive; access_ttl_secs and refresh_ttl_secs must each be no greater than absolute_session_ttl_secs.
# Operational impact: Changes quant-pivot-web authentication, token verification, and password-crypto boundary; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-web authentication, token verification, and password-crypto boundary
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
refresh_ttl_secs = 604800
# Field: web.jwt.absolute_session_ttl_secs
# Purpose: Absolute login-session lifetime in seconds (default 2592000 = 30d).
# Requirement: Required; recommended value: 2592000.
# Type/unit/range: Integer; seconds; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.web.jwt-ttl] When Always: All token TTLs must be positive; access_ttl_secs and refresh_ttl_secs must each be no greater than absolute_session_ttl_secs.
# Operational impact: Changes quant-pivot-web authentication, token verification, and password-crypto boundary; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-web authentication, token verification, and password-crypto boundary
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
absolute_session_ttl_secs = 2592000

[quant.workers]
# Field: quant.workers.report_schedule_poll_secs
# Purpose: `PostgreSQL` report schedule reconciliation cadence (seconds).
# Requirement: Required; recommended value: 1.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.report.lifecycle-positive] When Always: The report polling interval, run lease, heartbeat interval, and ad-hoc queue TTL must each be greater than zero. [deploy.report.ad-hoc-ttl] When Always: report_ad_hoc_queue_ttl_secs must be greater than report_schedule_poll_secs.
# Operational impact: Changes quant-pivot-core bounded report, reconciliation, and execution workers; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core bounded report, reconciliation, and execution workers
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
report_schedule_poll_secs = 1
# Field: quant.workers.report_run_lease_secs
# Purpose: Durable report-run lease duration (seconds).
# Requirement: Required; recommended value: 120.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.report.lifecycle-positive] When Always: The report polling interval, run lease, heartbeat interval, and ad-hoc queue TTL must each be greater than zero. [deploy.report.heartbeat-lease] When Always: report_run_heartbeat_secs must be no greater than one third of report_run_lease_secs.
# Operational impact: Changes quant-pivot-core bounded report, reconciliation, and execution workers; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core bounded report, reconciliation, and execution workers
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
report_run_lease_secs = 120
# Field: quant.workers.report_run_heartbeat_secs
# Purpose: Heartbeat cadence for a claimed report run (seconds).
# Requirement: Required; recommended value: 30.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.report.lifecycle-positive] When Always: The report polling interval, run lease, heartbeat interval, and ad-hoc queue TTL must each be greater than zero. [deploy.report.heartbeat-lease] When Always: report_run_heartbeat_secs must be no greater than one third of report_run_lease_secs.
# Operational impact: Changes quant-pivot-core bounded report, reconciliation, and execution workers; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core bounded report, reconciliation, and execution workers
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
report_run_heartbeat_secs = 30
# Field: quant.workers.report_ad_hoc_queue_capacity
# Purpose: Maximum queued ad-hoc report requests across all replicas.
# Requirement: Required; recommended value: 64.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.report.ad-hoc-capacity] When Always: The ad-hoc report queue capacity must be between 1 and 1024 inclusive.
# Operational impact: Changes quant-pivot-core bounded report, reconciliation, and execution workers; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core bounded report, reconciliation, and execution workers
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
report_ad_hoc_queue_capacity = 64
# Field: quant.workers.report_ad_hoc_queue_ttl_secs
# Purpose: Maximum time an ad-hoc request may remain queued (seconds).
# Requirement: Required; recommended value: 300.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.report.lifecycle-positive] When Always: The report polling interval, run lease, heartbeat interval, and ad-hoc queue TTL must each be greater than zero. [deploy.report.ad-hoc-ttl] When Always: report_ad_hoc_queue_ttl_secs must be greater than report_schedule_poll_secs.
# Operational impact: Changes quant-pivot-core bounded report, reconciliation, and execution workers; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core bounded report, reconciliation, and execution workers
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
report_ad_hoc_queue_ttl_secs = 300
# Field: quant.workers.report_expire_sweep_secs
# Purpose: Report TTL expiry cadence (seconds). Serves two roles for report expiry (`expires_at` deadline frozen at publish): the `ReportDeadlineScheduler` reconcile/look-ahead interval (precise per-report wakes) **and** the decoupled `ReportExpireSweep` backstop cadence. The DB is the source of truth; the scheduler is a latency optimization and the sweep is its durable safety net. Unrelated to report fire scheduling (owned by the durable `PostgreSQL` coordinator).
# Requirement: Required; recommended value: 300.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.report.expiry-sweep] When Always: The report expiry sweep interval must be greater than zero.
# Operational impact: Changes quant-pivot-core bounded report, reconciliation, and execution workers; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core bounded report, reconciliation, and execution workers
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
report_expire_sweep_secs = 300
# Field: quant.workers.intent_expire_sweep_secs
# Purpose: Order-intent TTL expiry cadence (seconds). Serves as the `IntentDeadlineScheduler` reconcile/look-ahead interval (precise per-intent wakes that release reserved capital at `expires_at`) **and** the `IntentExpireSweep` backstop cadence. Every fire re-checks the DB and runs `CoreOrderIntentService::expire_due` (atomic status + capital release), so a missed or duplicated wake only affects latency.
# Requirement: Required; recommended value: 60.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core bounded report, reconciliation, and execution workers; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core bounded report, reconciliation, and execution workers
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
intent_expire_sweep_secs = 60
# Field: quant.workers.execution_dispatch_secs
# Purpose: Auto-execution dispatcher poll-backstop cadence (seconds). The dispatcher is wake-driven: a fresh `ApprovedByPolicy` approval nudges it for near-immediate submit. This cadence is the **backstop** poll that catches missed wakes, retries admission defers, and drains crash-recovery work. The durable queue is Postgres (`ApprovedByPolicy` rows under a per-intent row lock). The current deployment runs a single dispatcher; repository locking remains authoritative across processes.
# Requirement: Required; recommended value: 10.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core bounded report, reconciliation, and execution workers; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core bounded report, reconciliation, and execution workers
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
execution_dispatch_secs = 10
# Field: quant.workers.execution_breaker_tick_secs
# Purpose: Execution-breaker self-heal tick cadence (seconds). Drives `ExecutionBreaker::tick`, which recovers `Degraded -> Healthy` after the configured cooldown. `Halted` is latched and never auto-heals.
# Requirement: Required; recommended value: 5.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core bounded report, reconciliation, and execution workers; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core bounded report, reconciliation, and execution workers
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
execution_breaker_tick_secs = 5
# Field: quant.workers.equity_snapshot_secs
# Purpose: Best-effort equity-history snapshot cadence (seconds). Report generation computes and persists its own authoritative equity snapshot synchronously. This worker only adds heartbeat/history points between reports.
# Requirement: Required; recommended value: 300.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core bounded report, reconciliation, and execution workers; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core bounded report, reconciliation, and execution workers
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
equity_snapshot_secs = 300
# Field: quant.workers.venue_incentive_reconciliation_secs
# Purpose: Venue maker-award and wallet-credit reconciliation cadence.
# Requirement: Required; recommended value: 3600.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core bounded report, reconciliation, and execution workers; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core bounded report, reconciliation, and execution workers
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
venue_incentive_reconciliation_secs = 3600
# Field: quant.workers.venue_incentive_lookback_days
# Purpose: Closed UTC days re-read on every incentive pass. Re-reading is intentional because venue awards can arrive after the program day.
# Requirement: Required; recommended value: 35.
# Type/unit/range: Integer; days; minimum=0.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-core bounded report, reconciliation, and execution workers; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core bounded report, reconciliation, and execution workers
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
venue_incentive_lookback_days = 35

[quant.account]
# Field: quant.account.funder
# Purpose: Polymarket proxy/funder address used as `user=<funder>` for Data API position reads. Required to generate reports (all modes).
# Requirement: Optional; recommended value: no implicit default.
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.account-read-credentials] When Whenever the production account is read: A private key and non-empty funder address are required because reports freeze real collateral and position state; account reads are not simulation.
# Operational impact: Changes quant-pivot-core credential-gated venue account snapshot and report capital freeze; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core credential-gated venue account snapshot and report capital freeze
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_identifier; deployment projection never exposes protected literals.
# Variants: not applicable.
funder = "REPLACE_WITH_POLYMARKET_FUNDER_ADDRESS"
# Field: quant.account.wallet_kind
# Purpose: Wallet shape for money-moving on-chain operations.
# Requirement: Required; recommended value: "eoa".
# Type/unit/range: Variant; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.relayer.credentials] When When runtime mode permits order submission and wallet_kind is proxy, gnosis_safe, or deposit_wallet: Both relayer API key and relayer API-key address are required.
# Operational impact: Changes quant-pivot-core credential-gated venue account snapshot and report capital freeze; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core credential-gated venue account snapshot and report capital freeze
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: variant_0, variant_1, variant_2, variant_3.
wallet_kind = "eoa"

[quant.research_jobs]
# Field: quant.research_jobs.global_concurrency
# Purpose: Maximum jobs executing concurrently across all kinds.
# Requirement: Required; recommended value: 2.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.global-concurrency] When Always: Global durable research-job concurrency must be between 1 and 32 inclusive.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
global_concurrency = 2
# Field: quant.research_jobs.dataset_build_concurrency
# Purpose: Per-kind concurrency cap for dataset builds (the heaviest kind).
# Requirement: Required; recommended value: 1.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.kind-concurrency] When Always: Each stage-specific concurrency must be greater than zero and no greater than global_concurrency.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
dataset_build_concurrency = 1
# Field: quant.research_jobs.model_train_concurrency
# Purpose: Per-kind concurrency cap for model-training runs.
# Requirement: Required; recommended value: 1.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.kind-concurrency] When Always: Each stage-specific concurrency must be greater than zero and no greater than global_concurrency.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
model_train_concurrency = 1
# Field: quant.research_jobs.backtest_concurrency
# Purpose: Per-kind concurrency cap for backtests.
# Requirement: Required; recommended value: 2.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.kind-concurrency] When Always: Each stage-specific concurrency must be greater than zero and no greater than global_concurrency.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
backtest_concurrency = 2
# Field: quant.research_jobs.bias_table_fit_concurrency
# Purpose: Per-kind concurrency cap for favorite-longshot bias-table fits.
# Requirement: Required; recommended value: 1.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.kind-concurrency] When Always: Each stage-specific concurrency must be greater than zero and no greater than global_concurrency.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
bias_table_fit_concurrency = 1
# Field: quant.research_jobs.model_calibration_fit_concurrency
# Purpose: Per-kind concurrency cap for model-score calibrator fits.
# Requirement: Required; recommended value: 1.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.kind-concurrency] When Always: Each stage-specific concurrency must be greater than zero and no greater than global_concurrency.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
model_calibration_fit_concurrency = 1
# Field: quant.research_jobs.cpcv_backtest_concurrency
# Purpose: Per-kind concurrency cap for CPCV + trial-grid validation runs — the second-heaviest kind after dataset builds (rayon-parallel internally, so a low cap of `1` keeps host CPU predictable).
# Requirement: Required; recommended value: 1.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.kind-concurrency] When Always: Each stage-specific concurrency must be greater than zero and no greater than global_concurrency.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
cpcv_backtest_concurrency = 1
# Field: quant.research_jobs.feature_parity_concurrency
# Purpose: Per-kind cap for deterministic feature-parity replay.
# Requirement: Required; recommended value: 1.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.kind-concurrency] When Always: Each stage-specific concurrency must be greater than zero and no greater than global_concurrency. [deploy.research.feature-parity-concurrency] When Always: max_concurrency must equal 1 and be no greater than feature_parity_concurrency.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
feature_parity_concurrency = 1
# Field: quant.research_jobs.feedback_coverage_concurrency
# Purpose: Per-kind cap for frozen feedback coverage scans.
# Requirement: Required; recommended value: 1.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.kind-concurrency] When Always: Each stage-specific concurrency must be greater than zero and no greater than global_concurrency.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
feedback_coverage_concurrency = 1
# Field: quant.research_jobs.feedback_drift_concurrency
# Purpose: Per-kind cap for statistical drift computation.
# Requirement: Required; recommended value: 1.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.kind-concurrency] When Always: Each stage-specific concurrency must be greater than zero and no greater than global_concurrency.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
feedback_drift_concurrency = 1
# Field: quant.research_jobs.feedback_learning_concurrency
# Purpose: Per-kind cap for each bounded feedback learning-stage batch.
# Requirement: Required; recommended value: 1.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.kind-concurrency] When Always: Each stage-specific concurrency must be greater than zero and no greater than global_concurrency.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
feedback_learning_concurrency = 1
# Field: quant.research_jobs.feedback_cycle_concurrency
# Purpose: Maximum feedback cycles concurrently owned by one resident coordinator.
# Requirement: Required; recommended value: 2.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.kind-concurrency] When Always: Each stage-specific concurrency must be greater than zero and no greater than global_concurrency.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
feedback_cycle_concurrency = 2
# Field: quant.research_jobs.trade_policy_fit_concurrency
# Purpose: Per-kind cap for executable trade-policy fits.
# Requirement: Required; recommended value: 1.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.kind-concurrency] When Always: Each stage-specific concurrency must be greater than zero and no greater than global_concurrency.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
trade_policy_fit_concurrency = 1
# Field: quant.research_jobs.trade_policy_validation_concurrency
# Purpose: Per-kind cap for independent row-level trade-policy validation.
# Requirement: Required; recommended value: 1.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.kind-concurrency] When Always: Each stage-specific concurrency must be greater than zero and no greater than global_concurrency.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
trade_policy_validation_concurrency = 1
# Field: quant.research_jobs.lease_ttl_secs
# Purpose: Lease time-to-live: a lease not renewed within this window is reclaimable.
# Requirement: Required; recommended value: 90.
# Type/unit/range: Integer; seconds; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.research.lease] When Always: The durable job lease TTL must be between 3 and 3600 seconds inclusive. [deploy.research.heartbeat] When Always: heartbeat_secs must be greater than zero and no greater than one third of lease_ttl_secs. [deploy.research.stuck-deadline] When Always: feedback_stuck_secs must be greater than lease_ttl_secs and no greater than 2592000 seconds (30 days).
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
lease_ttl_secs = 90
# Field: quant.research_jobs.heartbeat_secs
# Purpose: How often a running job renews its lease + emits a liveness heartbeat.
# Requirement: Required; recommended value: 30.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.research.heartbeat] When Always: heartbeat_secs must be greater than zero and no greater than one third of lease_ttl_secs.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
heartbeat_secs = 30
# Field: quant.research_jobs.poll_secs
# Purpose: Idle poll cadence when the queue is empty or capacity is saturated.
# Requirement: Required; recommended value: 3.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.research.poll] When Always: The durable job polling interval must be between 1 and 300 seconds inclusive.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
poll_secs = 3
# Field: quant.research_jobs.max_recovery_attempts
# Purpose: Bounded automatic crash-recovery re-queues before a job is quarantined.
# Requirement: Required; recommended value: 3.
# Type/unit/range: Integer; count; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: [deploy.research.recovery-attempts] When Always: The recovery-attempt limit must be between 0 and 32 inclusive.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_recovery_attempts = 3
# Field: quant.research_jobs.execution_retry_initial_secs
# Purpose: Initial durable retry delay for typed transient execution failures.
# Requirement: Required; recommended value: 2.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.research.execution-retry] When Always: execution_retry_initial_secs must be greater than zero and no greater than execution_retry_max_secs; execution_retry_max_secs must be no greater than 3600 seconds.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
execution_retry_initial_secs = 2
# Field: quant.research_jobs.execution_retry_max_secs
# Purpose: Maximum durable retry delay after exponential backoff.
# Requirement: Required; recommended value: 60.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.research.execution-retry] When Always: execution_retry_initial_secs must be greater than zero and no greater than execution_retry_max_secs; execution_retry_max_secs must be no greater than 3600 seconds.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
execution_retry_max_secs = 60
# Field: quant.research_jobs.max_spine_samples
# Purpose: Hard cap on the deterministic historical spine (selection × instants): a dry-run `plan` beyond this flags `hard_cap_exceeded`; a `build` fails closed.
# Requirement: Required; recommended value: 2000000.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.spine-budget] When Always: The frozen training-spine sample budget must be between 1 and 100000000 inclusive.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_spine_samples = 2000000
# Field: quant.research_jobs.plan_sample_slices
# Purpose: Number of `as_of` slices sampled during `plan` to estimate the PIT selection keep-rate (`0` disables the estimate).
# Requirement: Required; recommended value: 5.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.plan-sample-budget] When Always: plan_sample_slices must be no greater than 64 and plan_sample_markets must be between 1 and 10000 inclusive.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
plan_sample_slices = 5
# Field: quant.research_jobs.plan_sample_markets
# Purpose: Number of candidate markets sampled per slice for the keep-rate estimate (bounds the point-in-time reads a `plan` issues to `slices × markets`).
# Requirement: Required; recommended value: 200.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.plan-sample-budget] When Always: plan_sample_slices must be no greater than 64 and plan_sample_markets must be between 1 and 10000 inclusive.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
plan_sample_markets = 200
# Field: quant.research_jobs.progress_min_interval_ms
# Purpose: Minimum interval between throttled progress writes (DB heartbeat + WS push).
# Requirement: Required; recommended value: 500.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.research.progress-cadence] When Always: The durable progress-update interval must be between 50 and 60000 ms inclusive.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
progress_min_interval_ms = 500
# Field: quant.research_jobs.feedback_stuck_secs
# Purpose: Elapsed DB-clock age after which a running feedback cycle emits a deduplicated scheduler-health alert. The cycle remains durable and is never synthetically terminalized.
# Requirement: Required; recommended value: 21600.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.research.stuck-deadline] When Always: feedback_stuck_secs must be greater than lease_ttl_secs and no greater than 2592000 seconds (30 days).
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
feedback_stuck_secs = 21600
# Field: quant.research_jobs.feedback_alert_timeout_secs
# Purpose: Maximum time one feedback scheduler alert may occupy the coordinator.
# Requirement: Required; recommended value: 2.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.research.alert-timeout] When Always: feedback_alert_timeout_secs must be greater than zero and no greater than shutdown_drain_secs. [deploy.research.alert-dedupe] When Always: feedback_alert_dedupe_secs must be no less than feedback_alert_timeout_secs and no greater than 86400 seconds.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
feedback_alert_timeout_secs = 2
# Field: quant.research_jobs.feedback_alert_dedupe_secs
# Purpose: Durable-condition alert dedupe horizon.
# Requirement: Required; recommended value: 900.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.research.alert-dedupe] When Always: feedback_alert_dedupe_secs must be no less than feedback_alert_timeout_secs and no greater than 86400 seconds.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
feedback_alert_dedupe_secs = 900
# Field: quant.research_jobs.shutdown_drain_secs
# Purpose: Bounded grace period a graceful shutdown waits for in-flight jobs to cooperatively unwind at a stage boundary before their rows are explicitly re-queued for the next epoch. Keep short: the build restarts from scratch on re-lease, so draining only lets near-complete work finish.
# Requirement: Required; recommended value: 3.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.research.alert-timeout] When Always: feedback_alert_timeout_secs must be greater than zero and no greater than shutdown_drain_secs. [deploy.research.shutdown-drain] When Always: The shutdown drain budget must be between 1 and 3 seconds inclusive.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
shutdown_drain_secs = 3

[quant.research_jobs.feature_parity_compute]
# Field: quant.research_jobs.feature_parity_compute.page_size
# Purpose: Maximum frozen serving subjects replayed in one bounded page.
# Requirement: Required; recommended value: 100.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.feature-parity-page] When Always: The feature-parity page size must be between 1 and 1000 subjects inclusive.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
page_size = 100
# Field: quant.research_jobs.feature_parity_compute.max_concurrency
# Purpose: Maximum replay kernels admitted concurrently by the source.
# Requirement: Required; recommended value: 1.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.feature-parity-concurrency] When Always: max_concurrency must equal 1 and be no greater than feature_parity_concurrency.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_concurrency = 1
# Field: quant.research_jobs.feature_parity_compute.max_working_set_bytes
# Purpose: Logical peak working set reserved for each replay kernel.
# Requirement: Required; recommended value: 4294967296.
# Type/unit/range: Integer; bytes; minimum=0.
# Validation/cross-field contract: [deploy.research.feature-parity-memory] When Always: The memory budget must be between 1048576 bytes (1 MiB) and 10737418240 bytes (10 GiB) inclusive.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_working_set_bytes = 4294967296
# Field: quant.research_jobs.feature_parity_compute.deadline_secs
# Purpose: End-to-end wall-clock deadline for one replay attempt.
# Requirement: Required; recommended value: 1800.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.research.feature-parity-deadline] When Always: The stage deadline must be between 1 and 86400 seconds inclusive.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
deadline_secs = 1800

[quant.research_jobs.feedback_attribution_compute]
# Field: quant.research_jobs.feedback_attribution_compute.page_size
# Purpose: Maximum model-learning cohort rows loaded into one materialization page.
# Requirement: Required; recommended value: 250.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.attribution-page] When Always: The feedback-attribution page size must be between 1 and 1000 rows inclusive.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
page_size = 250
# Field: quant.research_jobs.feedback_attribution_compute.max_concurrency
# Purpose: Maximum asynchronous attribution groups in flight. CPU kernels remain exclusively governed by the process-wide compute executor.
# Requirement: Required; recommended value: 4.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.research.attribution-concurrency] When Always: Attribution-group concurrency must be between 1 and 32 inclusive.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_concurrency = 4
# Field: quant.research_jobs.feedback_attribution_compute.max_working_set_bytes
# Purpose: Logical peak working set reserved for each admitted kernel.
# Requirement: Required; recommended value: 536870912.
# Type/unit/range: Integer; bytes; minimum=0.
# Validation/cross-field contract: [deploy.research.attribution-memory] When Always: The memory budget must be between 1048576 bytes (1 MiB) and 10737418240 bytes (10 GiB) inclusive.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_working_set_bytes = 536870912
# Field: quant.research_jobs.feedback_attribution_compute.deadline_secs
# Purpose: End-to-end wall-clock deadline for one attribution-manifest job.
# Requirement: Required; recommended value: 1800.
# Type/unit/range: Integer; seconds; minimum=0.
# Validation/cross-field contract: [deploy.research.attribution-deadline] When Always: The stage deadline must be between 1 and 86400 seconds inclusive.
# Operational impact: Changes quant-pivot-core durable research-job workers and 15-stage feedback coordinator; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-core durable research-job workers and 15-stage feedback coordinator
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
deadline_secs = 1800

[quant.portfolio_solver]
# Field: quant.portfolio_solver.deadline_secs
# Purpose: Hard wall-clock limit for one global planner invocation, in seconds. Includes exact preparation, lexicographic optimization, uniqueness proof, and publishable leave-one-out reoptimization; excludes upstream scenario and executable-tier materialization.
# Requirement: Required; recommended value: 180.
# Type/unit/range: Integer; seconds; minimum=1, maximum=600.
# Validation/cross-field contract: [deploy.solver.deadline] When Always: The end-to-end global-planner deadline must be between 1 and 600 seconds inclusive; reaching it is a report failure, never an acceptable incumbent fallback. The default is a bootstrap liveness ceiling for the qualified 10000-tier/400-scenario/Top20 workload, not a universal latency SLO.
# Operational impact: Changes quant-pivot-research global HiGHS MILP optimizer and exact post-solve verifier; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research global HiGHS MILP optimizer and exact post-solve verifier
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
deadline_secs = 180
# Field: quant.portfolio_solver.threads
# Purpose: Deterministic `HiGHS` worker thread count.
# Requirement: Required; recommended value: 1.
# Type/unit/range: Integer; count; minimum=1, maximum=1.
# Validation/cross-field contract: [deploy.solver.threads] When Always: The value must equal 1 for deterministic serial HiGHS execution.
# Operational impact: Changes quant-pivot-research global HiGHS MILP optimizer and exact post-solve verifier; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research global HiGHS MILP optimizer and exact post-solve verifier
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
threads = 1
# Field: quant.portfolio_solver.max_tiers
# Purpose: Maximum discrete tiers accepted by one report run.
# Requirement: Required; recommended value: 10000.
# Type/unit/range: Integer; count; minimum=1, maximum=50000.
# Validation/cross-field contract: [deploy.solver.tier-budget] When Always: The executable-tier budget must be between 1 and 50000 inclusive.
# Operational impact: Changes quant-pivot-research global HiGHS MILP optimizer and exact post-solve verifier; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research global HiGHS MILP optimizer and exact post-solve verifier
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_tiers = 10000
# Field: quant.portfolio_solver.max_scenarios
# Purpose: Maximum promoted joint scenarios accepted by one report run.
# Requirement: Required; recommended value: 400.
# Type/unit/range: Integer; count; minimum=3, maximum=10000.
# Validation/cross-field contract: [deploy.solver.scenario-budget] When Always: The joint-scenario budget must be between 3 and 10000 inclusive.
# Operational impact: Changes quant-pivot-research global HiGHS MILP optimizer and exact post-solve verifier; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research global HiGHS MILP optimizer and exact post-solve verifier
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_scenarios = 400
# Field: quant.portfolio_solver.max_top_n
# Purpose: Maximum selected recommendations accepted by one publishable solve.
# Requirement: Required; recommended value: 20.
# Type/unit/range: Integer; count; minimum=1, maximum=1000.
# Validation/cross-field contract: [deploy.solver.top-n-budget] When Always: The publishable leave-one-out width must be between 1 and 1000 inclusive; the default 20 is part of the qualified 10000-tier/400-scenario/Top20 workload tuple.
# Operational impact: Changes quant-pivot-research global HiGHS MILP optimizer and exact post-solve verifier; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research global HiGHS MILP optimizer and exact post-solve verifier
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_top_n = 20

[research.artifact_store]
# Field: research.artifact_store.kind
# Purpose: Storage backend used for immutable research evidence and model artifacts.
# Requirement: Required; recommended value: "local".
# Type/unit/range: String; unitless; enum=local|s3.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-research immutable evidence and model artifact store; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research immutable evidence and model artifact store
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
kind = "s3"
# Field: research.artifact_store.bucket
# Purpose: S3 bucket that owns immutable evidence objects; empty for the Local backend.
# Requirement: Required; recommended value: "".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-research immutable evidence and model artifact store; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research immutable evidence and model artifact store
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_identifier; deployment projection never exposes protected literals.
# Variants: not applicable.
bucket = "quant-pivot-production-artifacts"
# Field: research.artifact_store.prefix
# Purpose: Object key prefix, or the Local filesystem root.
# Requirement: Required; recommended value: "./var/artifacts".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-research immutable evidence and model artifact store; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research immutable evidence and model artifact store
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_identifier; deployment projection never exposes protected literals.
# Variants: not applicable.
prefix = "evidence/"
# Field: research.artifact_store.region
# Purpose: AWS-compatible signing region used by the S3 client.
# Requirement: Required; recommended value: "us-east-1".
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-research immutable evidence and model artifact store; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research immutable evidence and model artifact store
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
region = "us-east-1"
# Field: research.artifact_store.endpoint
# Purpose: Optional S3-compatible service endpoint; omitted for the public AWS endpoint.
# Requirement: Optional; recommended value: no implicit default.
# Type/unit/range: String; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-research immutable evidence and model artifact store; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research immutable evidence and model artifact store
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: sensitive_endpoint; deployment projection never exposes protected literals.
# Variants: not applicable.
endpoint = "https://s3.internal.example.invalid"
# Field: research.artifact_store.path_style
# Purpose: Whether S3 requests use path-style bucket addressing instead of virtual-hosted style.
# Requirement: Required; recommended value: true.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-research immutable evidence and model artifact store; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research immutable evidence and model artifact store
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
path_style = false
# Field: research.artifact_store.require_object_lock
# Purpose: Requires bucket-level object lock before production evidence writes are admitted.
# Requirement: Required; recommended value: false.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-research immutable evidence and model artifact store; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research immutable evidence and model artifact store
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
require_object_lock = true
# Field: research.artifact_store.require_versioning
# Purpose: Requires bucket versioning before production evidence writes are admitted.
# Requirement: Required; recommended value: false.
# Type/unit/range: Boolean; unitless; no additional scalar restriction beyond the serialized Rust type.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-research immutable evidence and model artifact store; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research immutable evidence and model artifact store
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
require_versioning = true

[research.evidence_attestation]
# Field: research.evidence_attestation.signing_key
# Purpose: Active lowercase-hex encoded 32-byte keyed-BLAKE3 key.
# Requirement: Required; recommended value: "".
# Type/unit/range: String; unitless; write_only.
# Validation/cross-field contract: [deploy.evidence.key-separation] When When the evidence-attestation signing key is configured: The evidence-attestation signing key must be cryptographically independent from the JWT signing key.
# Operational impact: Changes quant-pivot-research evidence signing and verification boundary; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research evidence signing and verification boundary
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: secret; deployment projection never exposes protected literals.
# Variants: not applicable.
signing_key = "REPLACE_WITH_RESEARCH_EVIDENCE_ATTESTATION_SIGNING_KEY"
# Field: research.evidence_attestation.previous_signing_keys
# Purpose: Historical verification-only keys, newest first.
# Requirement: Required; recommended value: no implicit default.
# Type/unit/range: StringArray; unitless; write_only.
# Validation/cross-field contract: No additional semantic or cross-field rule beyond the scalar and type contract.
# Operational impact: Changes quant-pivot-research evidence signing and verification boundary; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research evidence signing and verification boundary
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: secret; deployment projection never exposes protected literals.
# Variants: not applicable.
previous_signing_keys = []

[research.model_serving_registry]
# Field: research.model_serving_registry.max_cached_contracts
# Purpose: Maximum successfully validated contracts retained in process.
# Requirement: Required; recommended value: 32.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.model-registry.cache] When Always: The champion/shadow contract cache must hold between 1 and 1024 contracts inclusive.
# Operational impact: Changes quant-pivot-research route-owned champion and shadow model registry; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research route-owned champion and shadow model registry
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_cached_contracts = 32
# Field: research.model_serving_registry.max_pending_loads
# Purpose: Maximum cache-miss callers admitted across active and queued loads.
# Requirement: Required; recommended value: 64.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.model-registry.pending-loads] When Always: max_pending_loads must be no less than max_concurrent_loads and no greater than 4096.
# Operational impact: Changes quant-pivot-research route-owned champion and shadow model registry; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research route-owned champion and shadow model registry
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_pending_loads = 64
# Field: research.model_serving_registry.max_concurrent_loads
# Purpose: Maximum distinct cold loads performing repository/object-store I/O.
# Requirement: Required; recommended value: 4.
# Type/unit/range: Integer; count; minimum=0.
# Validation/cross-field contract: [deploy.model-registry.load-concurrency] When Always: Concurrent model loads must be between 1 and 64 inclusive. [deploy.model-registry.pending-loads] When Always: max_pending_loads must be no less than max_concurrent_loads and no greater than 4096.
# Operational impact: Changes quant-pivot-research route-owned champion and shadow model registry; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research route-owned champion and shadow model registry
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_concurrent_loads = 4
# Field: research.model_serving_registry.load_timeout_ms
# Purpose: End-to-end deadline for one cold contract load.
# Requirement: Required; recommended value: 60000.
# Type/unit/range: Integer; milliseconds; minimum=0.
# Validation/cross-field contract: [deploy.model-registry.load-timeout] When Always: The model-load timeout must be between 1000 and 300000 ms inclusive.
# Operational impact: Changes quant-pivot-research route-owned champion and shadow model registry; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research route-owned champion and shadow model registry
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
load_timeout_ms = 60000
# Field: research.model_serving_registry.max_total_shadow_model_bytes
# Purpose: Total resident-memory reservation available to route-owned shadow bindings across all Buy routes.
# Requirement: Required; recommended value: 2147483648.
# Type/unit/range: Integer; bytes; minimum=0.
# Validation/cross-field contract: [deploy.model-registry.shadow-memory] When Always: The aggregate shadow-model memory budget must be between 67108864 bytes (64 MiB) and 1099511627776 bytes (1 TiB) inclusive.
# Operational impact: Changes quant-pivot-research route-owned champion and shadow model registry; the process must restart and pass fail-closed startup validation before the new value is consumed.
# Consumer: quant-pivot-research route-owned champion and shadow model registry
# Apply: process restart required; startup validation is fail-closed.
# Sensitivity/redaction: public; deployment projection never exposes protected literals.
# Variants: not applicable.
max_total_shadow_model_bytes = 2147483648

# Complete mutually exclusive tagged-union examples
# Select exactly one Polygon RPC source shape:
# polymarket.onchain.rpc_endpoint = { source = "public", url = "https://polygon-rpc.com" }
# polymarket.onchain.rpc_endpoint = { source = "protected", url = "REPLACE_WITH_AUTHENTICATED_POLYGON_RPC_URL" }
# Tornado scope variants are both instantiated in the canonical binding list:
# scope = { kind = "united_states" }
# scope = { kind = "state", spc_state_code = "OK", ncei_state_name = "OKLAHOMA" }
# Chainlink feed identifiers are subscription-specific; no fake fallback is active:
# [domain_sources.chainlink_data_streams.feeds."BTC-USD"]
# feed_id = "REPLACE_WITH_SUBSCRIPTION_V3_FEED_ID"
# decimals = 8
