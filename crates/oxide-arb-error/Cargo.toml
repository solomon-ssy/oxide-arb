[package]
name = "oxide-arb-error"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[features]
default = []
storage = [
    "dep:clickhouse",
    "dep:redis",
    "dep:deadpool-redis",
    "dep:bitcode",
]
config = ["dep:config"]
serde = ["dep:serde_json"]

[dependencies]
thiserror = { workspace = true }
sea-orm = { workspace = true }

# Storage-layer error types (behind feature flag to keep this crate lean)
clickhouse = { workspace = true, optional = true }
redis = { workspace = true, optional = true }
deadpool-redis = { workspace = true, optional = true }
bitcode = { workspace = true, optional = true }

# Configuration error types
config = { workspace = true, optional = true }

# Serialization error types (ApiError::Deserialize, WsError::MessageParse)
serde_json = { workspace = true, optional = true }

[lints]
workspace = true
