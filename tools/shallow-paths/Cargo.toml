[workspace]

[package]
name = "shallow-paths"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
syn = { version = "2", features = ["full", "visit", "visit-mut"] }
quote = "1"
proc-macro2 = { version = "1", features = ["span-locations"] }
walkdir = "2"
