# 10.1 — Frontend Domain Foundation

> 父：[`README.md`](README.md) · 契约：[`10.0-contract-and-deletion-inventory.md`](10.0-contract-and-deletion-inventory.md)
>
> 闭环定位：**types / API / WS / store 地基**。先切断旧 Endgame 领域依赖，再实现新业务页面。
> 10.1 完成前**不得**开始大规模页面实现。

## 1. 目标与闭环定位

交付五类编译地基：

1. **重建 `@vben/types`**：quant-pivot 类型模块 + enum/tag options；删除旧 Endgame export。
2. **新建 quant API modules**：按 domain 命名；统一 query/pagination/governed mutation。
3. **WS types/permission/dispatch 重写**：只允许 10.0 allowlist channel。
4. **Pinia store 边界重建**：cross-page/realtime/header state only。
5. **基础标签与格式化**：quant runtime/report/intent/execution tag options。

本子phase完成后，10.2 可直接建页面并 import 新 API/types，10.3+ 不再触碰旧 barrel。

## 2. 删除 / 合并 / 重构清单

### 2.1 删除

| 目标 | 动作 |
|---|---|
| `types/opportunity.ts` 等旧业务 type 文件 | **删除** |
| 旧 `index.ts` barrel export | **删除**旧业务 export |
| `ExecutionMode`、`EXECUTION_MODES`、`DryRun`、`Paper`、`Live` | **删除** |
| `api/opportunities.ts` 等 7 个旧 API module | **删除** |
| `store/opportunity.ts` 等 6 个旧 store | **删除** |
| 旧 WS cases 在 types/dispatch/permission | **删除** |
| `execution-mode-tag.vue`、`breaker-*` 等旧 badge | **删除** |

### 2.2 新增

| 模块 | 路径 |
|---|---|
| Types | `types/quant-report.ts`、`quant-recommendation.ts`、`order-intent.ts`、`execution-order.ts`、`position.ts`、`reconciliation.ts`、`settlement-redeem.ts`、`account.ts`、`research.ts`、`data-quality.ts`、`system.ts`、`ws.ts` |
| API | `api/quant-reports.ts`、`quant-recommendations.ts`、`order-intents.ts`、`execution-orders.ts`、`positions.ts`、`reconciliations.ts`、`settlement-redeems.ts`、`account.ts`、`research.ts`、`data-quality.ts`、`system.ts`、`markets.ts` |
| Store | `store/system.ts`、`market.ts`、`quant-report.ts`、`order-intent.ts`、`research.ts`、`ws.ts` |
| WS | `ws-channel-permissions.ts`、`ws-dispatch.ts`（重写） |
| Enums | `types/enums.ts` 中 quant runtime/report/execution/account/research tag options |

### 2.3 保留升级

- `shared/components/governed-action-modal.vue`
- `shared/composables/use-governed-action.ts`
- `shared/composables/use-oxide-access.ts`、`use-oxide-ws.ts`
- format helpers（去除旧业务命名）

## 3. 契约与模块设计

### 3.1 关键 TypeScript 类型

```typescript
type QuantRuntimeMode = 'report_only' | 'semi_auto' | 'auto_execution';

// 报告平面
interface QuantReportView { /* id, as_of, status, runtime_mode, top_n, ... */ }
interface QuantReportDetailView { /* trigger, source_delay, summary, ... */ }
interface RunReportRequest { reason: string; top_n?: number; source_delay_secs?: number; }
interface RunReportAccepted { request_id: string; trigger_key: string; }

// 推荐
interface QuantRecommendationView { /* rank, scores, plans, execution_eligibility, ... */ }
interface QuantEvidenceView { /* signal_candidate_id, book_snapshot_ref, ... */ }
interface RecommendationAttributionView { /* outcome, realized_pnl_usd, MAE/MFE, ... */ }

// 执行
interface OrderIntentView { /* status, approval_status, entry_order, exit_policy, ... */ }
interface CreateOrderIntentRequest { recommendation_id: string; reason: string; /* ... */ }
interface ExecutionOrderView { /* phase, venue_order_id, venue_status, state, ... */ }
interface PositionView { /* shares, avg_price, cost_usd, realized_pnl_usd, state, ... */ }
interface ReconciliationView { /* result, evidence_json, ... */ }
interface SettlementRedeemView { /* batch status, lots, ... */ }

// 账户
interface LiveAccountView { /* collateral, credential status, checked_at, ... */ }
interface EquitySnapshotView { /* equity, drawdown, ... */ }

// 系统
interface SystemStatus { /* quant_mode, kill_switch, health, ... */ }
interface KillSwitchView { /* state, changed_by, reason, ... */ }

// WS — server frame wire shape is { type, timestamp, data } (见 10.0 §3.3)；
// server control frames 复用 type: 'sync' | 'pong' | 'error'。没有 wire-level revision。
type WsServerKind = WsChannel | 'sync' | 'pong' | 'error';
interface WsFrame<K extends WsServerKind = WsServerKind> {
  type: K;
  timestamp: string; // RFC3339 millis
  data: K extends WsChannel ? WsChannelPayloads[K] : unknown;
}
type WsChannel = 'system.status' | 'system.alert' | 'market.resolved' | 'market.book_update'
  | 'config.activated' | 'quant.report' | 'quant.intent' | 'materialization.run_update';
```

**硬规则：** 所有 money/price/shares/bps/decimal wire 字段类型为 `string`，禁止 `number`。

### 3.2 API module 设计

每个 module 结构：

```typescript
// api/quant-reports.ts
export function listQuantReports(query: QuantReportListQuery): Promise<Paged<QuantReportView>>;
export function getLatestQuantReport(): Promise<QuantReportView>;
export function getQuantReport(id: string): Promise<QuantReportDetailView>;
export function runQuantReport(body: RunReportRequest): Promise<RunReportAccepted>; // governedPost
export function revokeQuantReport(id: string, body: RevokeReportRequest): Promise<void>; // governedPost
```

**`governedPost` 约定：**

- 必传 `reason` body 字段。
- 必传 header `X-Acting-Role`。
- 409/403/422 错误 detail 透传到 UI（不在 API 层吞掉）。

**`api/index.ts`：**

- 只 export 新业务 module + 保留基础 module（users/roles/menus/operation-log/runtime-config）。
- 零旧 Endgame export。

### 3.3 WebSocket 层设计

**`ws-channel-permissions.ts`：**

```typescript
const CHANNEL_PERMISSIONS: Record<WsChannel, string | string[]> = {
  'system.status': 'system:read',
  'market.book_update': 'market:read', // + scoped market_id
  // ...
};
```

**`ws-dispatch.ts`：**

- 按 channel 分发到对应 store action。
- 旧 channel case **不存在**（编译期 exhaustiveness）。
- reconnect 流程：`onConnected` → subscribe authorized → `sync` → restore market scoped。

**Store side effects：**

| Channel | Action |
|---|---|
| `system.status` | `systemStore.applyStatus` |
| `quant.report` | `quantReportStore.bumpRevision` |
| `quant.intent` | `orderIntentStore.bumpRevision` |
| `market.book_update` | `marketStore.setBook` |
| `materialization.run_update` | `researchStore.bumpRevision` |

### 3.4 Pinia store 设计

| Store | State | 禁止 |
|---|---|---|
| `system.ts` | status, kill-switch hint, mode revision, active config hint | 旧 PnL/breaker/trade balance |
| `market.ts` | scoped book cache, subscription refs | 全量 market list |
| `quant-report.ts` | last event, revision counter | report list |
| `order-intent.ts` | last event, revision counter | intent list |
| `research.ts` | materialization run event, revision | dataset/model catalog |
| `ws.ts` | connected, reconnecting, lastError | — |

页面表格数据：**页面级 query**（`useQuery` / composable），不进全局 store。

### 3.5 Enum tag options

新增 locale-backed tag options：

- `enum.quantRuntimeMode.*`
- `enum.killSwitchState.*`
- `enum.reportStatus.*`
- `enum.intentStatus.*`
- `enum.approvalStatus.*`
- `enum.executionOrderState.*`
- position state、reconciliation result 等

删除：`enum.executionMode.*`、旧 breaker/trade tag。

## 4. 推进计划

### 4.1 10.1.1 重建 `@vben/types`

**范围：** §3.1 全部 type 文件；重写 `enums.ts`；清理 barrel。

**验收：**

- `@vben/types/src/index.ts` 不 export 旧 Endgame 类型。
- `ExecutionMode` 不存在。
- decimal wire fields 不出现 `number`。
- `pnpm -F @vben/types run typecheck` 通过。

### 4.2 10.1.2 新建 quant API modules

**范围：** §3.2 全部 module；更新 `api/index.ts`。

**验收：**

- 旧 API module 零 import（grep 验证）。
- `governedPost` mutation 都传 `reason` 和 `X-Acting-Role`。
- API 路径与后端 route 一致（对照 10.0 matrix）。

### 4.3 10.1.3 WS types/permission/dispatch 重写

**范围：** §3.3 三层文件 + `use-oxide-ws.ts` 接入。

**验收：**

- 旧 WS channel 在类型、dispatch、测试中均不存在。
- 未授权 channel 不订阅。
- market scoped subscription reconnect 恢复逻辑有 unit test stub。

### 4.4 10.1.4 Pinia store 边界重建

**范围：** §3.4 六个 store；删除旧 store。

**验收：**

- report/intents store 只有 revision 和 last event。
- market store 可保存当前 market book cache。
- system store 不含旧字段。
- 旧 store 文件已删除。

### 4.5 10.1.5 基础标签与格式化

**范围：** §3.5 tag options + format helpers 清理。

**验收：**

- 枚举显示统一走 tag options。
- tag 文案通过 locale，无硬编码。
- 旧 breaker/execution-mode badge 已删除。

## 5. 生产不变量与 UX 语义

1. **`Accept-Api-Version: v1`** 请求头策略保持不变。
2. **`@vben/request`** 接入层不变；新 API 走同一 client。
3. Store 边界：WS 只 bump revision，页面自行 refetch list。
4. 类型层即文档：不允许 `any` 绕过旧 channel。

## 6. 验收测试

**Unit（10.1 最小集，完整集在 10.6）：**

- [ ] WS dispatch：每个 allowed channel 的 store side effect
- [ ] WS permission mapping：未授权不订阅
- [ ] governed-action composable：acting role/reason 必填

**Build：**

```bash
pnpm -F @vben/web-antdv-next run typecheck
pnpm -F @vben/web-antdv-next run build
```

## 7. Blocker

- 旧 type/API/store 仍被任何 `src/` 文件 import → **Blocker**
- `ExecutionMode` 或旧 WS channel 仍存在于 types → **Blocker**
- decimal 字段出现 `number` → **Blocker**
- 全局 store 保存分页表格数据 → **Blocker**

## 8. 延后 / 缺口

- 页面 UI **延后**至 10.2+。
- menu seed 同步 **延后**至 10.2。
- E2E **延后**至 10.6。
- locale 新增 namespace 可与 10.2 页面同步落地（10.1 先立 enum key 结构）。
