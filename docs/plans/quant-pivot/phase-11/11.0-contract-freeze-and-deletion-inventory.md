# 11.0 — Contract Freeze & Deletion Inventory

> 关闭审计点:**#12(build/feature-gate 缺陷)**、**#23(死语义)**。是全 Phase 11 的语义精准地基。
> 本子phase**不写业务逻辑**,只冻结契约、删除死语义、硬化 build/feature-gate、落地 CI 门禁。
>
> **状态:已落地(LANDED)。** 落地时相对本文初稿的关键决策(以代码为准):
> 1. runtime-config schema 代码基线已是 v10,Phase 11 冻结 bump 为 **v11**(非 v9→v10);新增 `research`/`feedback` 空段骨架,拒绝 <11(零兼容,无 shim)。
> 2. 空报告原因类型实际名为 `EmptyReason`,已**改名为 `EmptyReportReason`**(与 docs/前端一致);`AvailableCashExhausted` 升为一等变体并停止坍缩;`SolverUnavailable` → `SystemDegraded`。
> 3. 初稿漏列的两个 `EmptyReason` 死变体 `ModelQualityGateFailed` / `RuntimeModeDisabled`(均无 producer,且 publish gate 已保证 active 模型 passed / 无运行期禁用场景)**本期删除**;真需要时分别在 11.5 / 未来 pause 能力再加回。
> 4. `BindingConstraint` 死变体除 `DrawdownCap` 外,初稿漏列 `ConfidenceCap`(→11.3 与 drawdown 一并 emit)与 `ManualCap`(**本期删除**,无 manual-override 语义)。
> 5. 依既有 fail-closed 约束(报告平面 active 模型故障=硬失败+告警,而非静默空报告),**不引入** `assert_production_features()` / `InfraError::MissingBuildFeature`;改为 `quant-pivot-bin` **默认 features 即生产集**(`lp-solver,optimize,ml-classical`;`dataframe` 经 core→research 依赖恒链接)+ CI production-profile 构建 job 保证部署完整性。
> 6. `CalibrationArtifactId` 等 typed ID 骨架**不在本期预声明**(未构造即死代码),改由 owning sub-phase(11.3/11.5/11.9)在真正建能力时声明。

## 1. 目标与闭环定位

在 Phase 11 大规模改因子/模型/退出/闭环之前,先把"契约地基"钉死,避免后续子phase各自造轮子或
留死语义。三件事:

1. **删除死语义**:任何 enum 值/config 字段/DTO 字段,凡"写了但生产路径从不 emit/消费",要么在本
   Phase 有明确实现归属并登记,要么**立即删除**(零容忍向前兼容)。
2. **build/feature-gate 硬化**:部署二进制必须显式链接 `optimize`/`ml-classical`/`lp-solver`;缺失时
   **启动即失败**(fail-closed),绝不允许"报告成功但 0 条推荐"这种静默降级。
3. **锁定 runtime-config schema v10 与新枚举**:Phase 11 所有子phase新增的 config 段/enum 在此统一
   声明骨架,后续子phase只填语义,不再改 schema 版本号。

闭环定位:它是 Phase 11 依赖图的根节点(11.0 → 全部)。

## 2. 删除 / 合并 / 重构清单

### 2.1 死语义删除(#23,逐条给出处置)

| 死语义符号 | 现状 | Phase 11 处置 | 归属 |
|---|---|---|---|
| `BindingConstraint::DrawdownCap`(`enums/quant.rs`) | 枚举有值,`portfolio` 从不 emit;drawdown 只缩放 Kelly fraction | **登记待实现**:11.3 把 drawdown 作为独立 binding 约束 emit(见 11.3 §5);在 `dead-semantics-allowlist.txt` 登记 | 11.3 |
| `BindingConstraint::ConfidenceCap`(`enums/quant.rs`) | 枚举有值,从不 emit;confidence 只缩放 Kelly fraction | **登记待实现**:11.3 把 confidence-floor 作为独立 binding 约束 emit;allowlist 登记 | 11.3 |
| `BindingConstraint::ManualCap`(`enums/quant.rs`) | 枚举有值,无 manual-override 语义,从不 emit | **已删除(11.0)** | 11.0(删除) ✅ |
| `CapitalAllocationState::Planned`(`enums/execution.rs`) | 仅 ClickHouse 投影出现,live 写路径从 `Allocated` 起 | **已删除(11.0)**:报告不预留 venue 资本,sizing 只在 report 时点计算。同步删 pg enum / `ChCapitalAllocationState`(重编号 1..5)/ CH DDL / projection arm | 11.0(删除) ✅ |
| `EmptyReason::ModelQualityGateFailed` | 无 producer;publish gate 已保证 active 模型 passed=true,report 时点结构性不可能失败 | **已删除(11.0)**;11.5 若引入 report-time 重评则再加回 | 11.0(删除) ✅ |
| `EmptyReason::RuntimeModeDisabled` | 无 producer;三种 runtime mode 均产报告,无禁用场景 | **已删除(11.0)**;未来若引入运营级 pause/kill-switch 再加回 | 11.0(删除) ✅ |
| 报告 `superseded` 状态(设计文档提及,`enums/quant.rs` 无) | 概念存在,无枚举、无实现;新报告不自动作废旧报告 | **实现**:11.8 引入 `Superseded` 状态 + 自动作废 | 11.8 |
| `EntryTriggerKind::{Pullback,Breakout,TimeWindow,DataEvent}` | 枚举有值,composer 从不 emit,只用 `LimitPrice`/`Immediate` | **登记待实现**:11.7 让 composer 真正 emit(见 11.7 §5);allowlist 登记 | 11.7 |
| `ExitPlan.partial_exit_nodes / trailing_stop / signal_invalidation_rules` | schema 支持,composer 恒空 | **登记待实现**:11.7 由 composer 生成;allowlist 备注登记 | 11.7 |
| `EntryPlan` 缺 `confirmation_window_secs` | 设计文档 §8 有,类型无字段 | **新增字段 + 实现**:11.7;allowlist 备注登记 | 11.7 |

> 规则:表中"实现/登记待实现"项由对应子phase交付,并在 [`dead-semantics-allowlist.txt`](dead-semantics-allowlist.txt) 登记 owner phase;**若对应子phase最终决定不实现,则必须回到 11.0 删除该符号**。严禁"永久保留但从不 emit"。allowlist 中任一变体一旦被 emit(在生产 src 出现全限定 `Type::Variant`),`scripts/lint-dead-semantics.sh` 即 CI red,强制从 allowlist 移除。

### 2.2 空报告原因语义修复(#12 关联)— 已落地 ✅

`report/builder.rs` 中 `AvailableCashExhausted` 曾被坍缩映射到 `NoPositiveSignal`。**已破坏式修复**:
`EmptyReportReason`(原 `EmptyReason`,已改名)新增 `AvailableCashExhausted` 一等变体,一路透传到
report summary / API / UI(前端 banner 复用 `$t('enum.emptyReportReason.*')` 自动渲染),operator 可
区分"没现金"与"没信号"。`empty_reason_from_planner_rejections` 现按优先级映射:
`SolverUnavailable`→`SystemDegraded`(检查 `plan_row.optimizer_meta_json.status`)> `BudgetExhausted`→
`PortfolioBudgetExhausted` > `AvailableCashExhausted`→`AvailableCashExhausted` > 其余→`NoPositiveSignal`。
删除了坍缩映射与其单测,新增独立语义单测。最终 6 个变体各有独立 producer(零 wire-only 占位)。

### 2.3 无需删除

不删除既有 factor/feature/model 计算模块(11.1–11.7 在其上重构),不删除执行/对账/结算平面。

## 3. 新领域类型 / 表 / ClickHouse fact

本子phase只声明骨架(空实现留后续子phase),不新增业务表。落地:

- `EmptyReportReason`(models `enums/quant.rs`,原 `EmptyReason` 改名):`EmptySelection` /
  `InsufficientDataQuality` / `PortfolioBudgetExhausted` / `AvailableCashExhausted` / `NoPositiveSignal` /
  `SystemDegraded` — 每个都在 builder 有独立触发点。
- typed IDs(`CalibrationArtifactId`、`AttributionFeedbackRunId`、`FactorGovernanceAuditId`、
  `BacktestPathSetId`)**不在本期预声明**:未构造的 pub newtype 即死代码,违背零死代码。改由 owning
  sub-phase 在建能力时于 `types/ids.rs` 声明:11.3 `CalibrationArtifactId`、11.5 `BacktestPathSetId`、
  11.9 `AttributionFeedbackRunId` + `FactorGovernanceAuditId`(见各 §3)。

## 4. deploy-config key 与 runtime-config schema path

### 4.1 runtime-config schema v11(骨架冻结)— 已落地 ✅

代码基线已是 v10(本文初稿假设的 v9 已过时)。Phase 11 冻结 bump 为 **v11**,新增段(空默认,后续
子phase填字段):

```text
runtime_config (v11)
├── selection            (既有)
├── data_quality         (既有)
├── features             (既有;11.1 扩展 normalization 段)
├── factors              (既有;11.1 改默认 + 新增 normalization/orthogonalization)
├── model                (既有;11.3 新增 calibration 段)
├── quality_gate         (既有)
├── training             (既有)
├── reports              (既有;11.8 新增 lifecycle 段)
├── portfolio            (既有;11.3 新增 kelly_safety 段)
├── execution            (既有;11.7 新增 order_type_policy;11.11 新增 preflight_probes)
├── notification         (既有)
├── research             (新增空段;11.4 训练目标 + 11.5 验证方法论 config)
└── feedback             (新增空段;11.9 attribution feedback + auto-retrain config)
```

v11 一次性引入,后续 11.1–11.11 只在既有段内加字段,**不再 bump schema 版本**(除非破坏式)。
不写 v10→v11 迁移 shim;<11 config 直接拒绝加载(零兼容,`from_json` + activation preflight 双门)。
`config/quant-pivot.toml` 是 **deploy** config(runtime-config 存 Postgres、由 `RuntimeConfig::default()`
在 bootstrap 播种),故此处无需改;仅修正陈旧注释。`research`/`feedback` 为空 closed object,schema
walker 通过 **11.0 临时跳过逻辑**不产 leaf(见
[`json_schema/fields.rs`](../../../../crates/quant-pivot-models/src/runtime_config/json_schema/fields.rs)
`walk_schema` 内 `is_empty_closed_object` 早退,约 135–141 行 + 辅助函数 164–175 行),故 preferences
UI 无需登记直到后续子phase加字段。两段在 `dead-semantics-allowlist.txt` 备注登记 owner。
**删除义务**:11.4/11.5 填 `research` 后该段自动走 properties 分支,**仍须保留** skip 逻辑(`feedback`
仍空);**11.9 填 `feedback` 后必须整段删除**(见 11.9 §2),不得遗留死代码。

### 4.2 deploy-config(build/feature 硬化,#12)— 已落地(调整方案)✅

- `quant-pivot-bin` 的 **默认 features 即生产集**:`default = ["lp-solver","optimize","ml-classical"]`
  (`dataframe` 经 `quant-pivot-core → quant-pivot-research` 依赖恒链接,无需 bin flag;`calibration`
  于 11.3 引入)。默认构建即完整,dev/CI/deploy 一致。
- **不引入** boot `assert_production_features()` / `InfraError::MissingBuildFeature`(依既有 fail-closed
  约束:报告平面 active 模型/组合器真实运行期故障已是硬失败+critical alert,而非静默空报告;`SolverUnavailable`
  现映射 `SystemDegraded`)。部署完整性由"默认即生产集 + CI production-profile 构建 job"保证。
- CI 新增 `production-profile` job:`cargo build -p quant-pivot-bin`(默认)与显式
  `--features lp-solver,optimize,ml-classical` 均须构建通过;并在 `check` job 加挂
  `lint-quant-pivot-errors.sh`(此前遗漏)与 `lint-dead-semantics.sh`。

## 5. 必建模块与 trait

本子phase核心产物是**CI 门禁脚本 + build 断言**,非业务 trait。

### 5.1 `scripts/lint-dead-semantics.sh`(新增)

职责:静态扫描,任何 `enum` variant / `pub` config 字段 / DTO 字段,若在 `src/`(非 test)无任何
`emit`/构造/读取点,则报错。实现方式:

- 维护一份 `docs/plans/quant-pivot/phase-11/dead-semantics-allowlist.txt`(仅允许"外部 wire 协议
  必须存在但本地不 emit"的极少数例外,每条带理由注释)。
- 脚本用 `rg` 找每个 variant 的构造点计数;计数为 0 且不在 allowlist → 失败。
- 纳入 §7 质量门禁与 CI。

### 5.2 build 断言 — 已取消(改默认即生产集)

原设计的 `assert_production_features()` / `InfraError::MissingBuildFeature` **不落地**。理由:默认 features
已是生产集(§4.2),部署二进制恒完整;且报告平面真实运行期故障已 fail-closed(硬失败+告警,非静默空
报告)。保留一层 boot 断言反而与"默认即生产"重复。部署完整性由 CI production-profile job 守护。

## 6. 生产不变量与失败语义

- **零死语义**:合并入 CI 后,任何新增"从不 emit 的 variant/字段"直接 CI red。
- **build fail-closed**:缺 feature → 启动失败,不降级。
- **schema 零兼容**:v9 config 加载直接 `ConfigValidation` 错误,不 shim。
- **空报告原因精准**:五种 `EmptyReportReason` 各有独立触发点与文案,禁止坍缩。

## 7. 第三方 crate 引入

无新增运行时 crate。CI 脚本使用既有 `rg`。schema 校验沿用 `schemars`。

## 8. 验收测试

- `empty_reason_keeps_available_cash_distinct_from_no_signal` / `empty_reason_maps_solver_unavailable_to_system_degraded`
  (builder 单测,已加)。
- `rejects_prior_schema_documents`(加载 v10 JSON 断言 `UnsupportedSchemaVersion`,已改)。
- `schema_version_is_current`(断言 == 11,已改)。
- `capital_allocation_state_planned_removed` / 两个 `EmptyReason` 死变体删除 / `ManualCap` 删除 —
  由删除本身在编译期保证(引用不通过);`scripts/lint-dead-semantics.sh` 回归守卫防其复现。
- boot feature 断言测试:**已取消**(无 boot 断言);改由 CI production-profile 构建 job 覆盖。

## 9. Blocker

- 存在任一"实现归属未定"的死语义符号(既不实现也不删除)。
- production build 缺 feature 却能启动。
- v9 config 仍可加载。

## 10. 延后 / 缺口

- 死语义的**具体实现**(DrawdownCap→11.3、superseded→11.8、EntryTriggerKind/退出结构→11.7)在各自
  子phase交付;11.0 只负责登记 + 删除"确定不实现"的项 + 上 CI 门禁。
- **`json_schema/fields.rs` 空段跳过逻辑(11.0 临时)**:11.4/11.5 向 `ResearchConfig` 加字段后
  `research` 不再命中 `is_empty_closed_object`,但 **`feedback` 仍空,skip 须保留**;**11.9 向
  `FeedbackConfig` 加字段后必须删除** `walk_schema` 内早退块(约 135–141 行)与 `is_empty_closed_object`
  辅助函数(约 164–175 行)及关联单测——v11 再无空 closed object 段,遗留即死代码。
- 对象存储 artifact、S3 URI 迁移仍延后(与 phase-03 一致)。
- 回指审计点:#12、#23 在此闭合骨架,具体实现分散到 11.1–11.11。
