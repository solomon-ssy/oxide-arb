# 11.9 — Attribution Feedback、Auto-Retraining 与 Model Route Governance

<!-- quant-pivot-deployment-contract:v1 -->
> **Deployment contract**
> - `fresh_boot_assumption`: 项目尚未正式生产上线，从全新 PostgreSQL/ClickHouse schema version 1
>   部署。
> - `schema_data_version_impact`: 不迁移 fixture、旧 lifecycle 或旧 artifact；不保留 alias、
>   compatibility wrapper、re-export、dual read/write 或 legacy parser。
> - `pre_deployment_behavior`: clean break 与 fresh bootstrap 合法；任何已有真实数据销毁仍需操作者
>   单独授权。
> - `post_deployment_behavior`: 首次部署后恢复正常备份、前向 migration、验证与显式回滚纪律。
> - `deployment_topology`: AWS 单应用实例。PostgreSQL 仍是 durable state、transaction、WORM、
>   lease/retry 和 crash-recovery authority；不建设 leader election、replica membership 或跨实例
>   convergence protocol。

> 状态：**Phase 11.9 Implementation Closure=DONE**。2026-07-31 已从 current bytes
> 重新完成 §10 全部门禁；该结论不等于任一真实 route 已完成 `Operational Activation`。
>
> 最后更新：2026-07-31
>
> 审计点：#19、#20、#21。原 11.10 已
> [MERGED INTO 11.9](11.10-counterfactual-factor-attribution.md)，不再是独立实现或依赖节点。
>
> 旧关闭流水只保存在
> [历史实施归档](11.9-implementation-history-2026-07-30.md)，不得作为本轮 closure evidence
> 继承。

## 0. 当前检查点

- `baseline_branch`: `quant-pivot`
- `baseline_head`: `f7285d51b9a21304e36e2eb226819d0686459c9f`
- `working_tree_summary`: Phase 11.9 clean-break remediation 与 current-byte closure 已完成；root/UI 当前均为 dirty worktree，保留用户与既有实现改动，未 stage/commit/push
- `current_task_id`: `W5-08`
- `current_task_objective`: 固化完整 workspace、schema、fresh-stack、fault-injection 与 UI current-byte closure evidence
- `dependency_status`: `W5-01..W5-08=DONE`
- `changed_paths`: canonical truth、execution attempt/rollup、feedback scheduler/DAG、attribution、Weather 8-family、model manifest/route governance、REST/WS/UI、schema 与 Phase 文档
- `last_passed_command`: disposable PostgreSQL 16 `manifest-clean`、Rust full workspace gates、UI lint/typecheck/589 unit tests/production build、两次独立 fresh-stack browser runs、144-state responsive/Axe matrix、Rust↔TypeScript schema fixed-point 与 root/UI diff-check 全部 PASS
- `last_failed_command_and_root_cause`: 最终 UI matrix 发现 Antdv 按钮从 disabled 切换为 enabled 时用 `transition: all` 插值低透明度禁用色，产生短暂低对比帧；状态色改为原子切换并显式绑定 `--primary-foreground` 后，完整 144-state matrix PASS
- `evidence_artifacts_and_hashes`: PostgreSQL semantic manifest SHA-256=`dff329f9b716b6623b12e1173434bf0a936d7c03279b93a0b124778557e1f465`；migration manifest SHA-256=`f0e3ce8b01de7ea5ede516a0aac373fcd505303b11052a355a0364b1ba157c39`；research/config API schema SHA-256=`a7bccfd7d341fef7c8eb4f75c0454c86e1a9904e3ae08ab73b713337d219c417`/`a5cfc188b78b5c20016e56542d1da8cbeda06bba4ecc9fe6ea8b044e36947a0e`；responsive evidence aggregate SHA-256=`043e43713a40bca6e362ce5f454e16efe536d2dba74b74842c3ca0fd0ccfd308`
- `active_long_running_command`: 无
- `exact_resume_command`: 无；Implementation Closure 已完成
- `next_single_action`: AWS 首次部署后，只在 §9 的 current-data、mature-label 与完整 ReportOnly shadow 条件满足时启动单 route Operational Activation
- `updated_at`: `2026-07-31T08:24:17+08:00`

## 1. 当前结论

| 判定 | 状态 | 恢复条件 |
|---|---|---|
| `Implementation Closure` | `DONE` | 2026-07-31 current-byte canonical data、scheduler、attribution、Weather、governance、UI、schema、fault replay 与完整工程门禁全部通过 |
| `Operational Activation` | `BLOCKED` | authenticated current data、profile mature-label gate、至少一个完整 `retraining_cooldown` 的真实 ReportOnly shadow、人工 permit 与 route activation |

本阶段不扩大 `SemiAuto` / `AutoExecution`、capital、private-key、signing 或 funder authority。自动化
最多运行到 `CandidateReady`；permit 与 activation 永远是两个显式、已认证、可审计的人工动作。

## 2. Canonical truth 与 outcome

### 2.1 ResolutionObservationInbox

- PostgreSQL `quant_resolution_observation_inbox` 是 source observation 的 WORM inbox。
- 同一事务写入完整 source page、raw hash/URI、provider revision、cursor advance 和 projection
  outbox；ClickHouse 只保存 canonical projection，不是第二 truth。
- 未知 market 进入 typed `PendingMapping` / `Quarantined`，保留 observation 并持续重试；linkage 完成
  后 exactly-once 投影。禁止跳过 observation 后推进 cursor。
- revision、crash point、ClickHouse outage 和 mapping replay 必须保持 observation 零丢失、
  canonical fact 零重复。

### 2.2 ExecutionAttemptOutcome 与 RecommendationExecutionRollup

- `ExecutionAttemptOutcome` 以 `order_intent_id` 为唯一身份，封存该 intent 的 immutable terminal
  order/position/economics graph。
- `RecommendationExecutionRollup` 只有在 recommendation 不能再创建新 intent，且全部
  intent/order/position terminal 后才能封存。
- rollup 按确定顺序绑定全部 attempt hash 与聚合经济指标；feedback execution cohort 只消费最终
  rollup，不消费“首个成功 attempt”。
- reconciliation due/retry/lease 均在 PostgreSQL。单实例进程重启、永久 deferred 首项和 ClickHouse
  暂时失败不能饿死后续任务；`FOR UPDATE SKIP LOCKED` 用于数据库队列安全，不代表部署多副本。

### 2.3 TruthFreeze

每个 cycle 在进入 coverage 前必须封存 resolution inbox、attempt projector 和 recommendation rollup
三类 watermark。任何影响 cohort 的 quarantine、unresolved mapping、projection lag 或 rollup lag
都产生 typed blocker；未知值不能显示或参与计算为 0。

## 3. Auto-Retraining 与统计治理

唯一 DAG：

```text
Trigger → TruthFreeze → Coverage → AttributionPlan → Drift → DatasetSeal
        → Training → Calibration → CPCV → Validation → Comparison → Shadow → Decision
```

- scheduler state 由 PostgreSQL clock 和
  `next_due_at / last_cycle_id / cooldown_until / lease / attempt / retry_at / pause_revision`
  决定。AWS 单实例只运行一个 resident scheduler。
- Crypto cadence/cooldown 为 6h/3d；Weather 为 24h/14d。Scheduled 与 Manual 在相同
  `profile + cadence_cutoff` 下收敛到同一 cycle；触发来源写 append-only provenance。
- 普通 manual trigger 同样受 cooldown；失败使用原 identity retry。pause/resume 必须带 CAS、
  reason code 与 note。
- `Validation` 只调用唯一 `ModelQualityGate`。完整 trial universe 进入 CPCV、DSR、PBO 和
  multiple-testing accounting；未通过者不得进入 Comparison、Shadow 或 CandidateReady。
- quality report 覆盖 No-PIT、coverage、rank IC、DSR、PBO、turnover/tail loss、calibration、
  feature parity 与 explainability。
- scheduler 和 manual trigger 都不会签发 permit 或改变 serving route。

## 4. Attribution 的不可混淆语义

| Artifact | 解释对象 | 禁止宣称 |
|---|---|---|
| `PredictionExplanationArtifact` | 模型分数；weighted 使用精确闭式分解，tree ensemble 使用 exact TreeSHAP | 现实因果效应、PnL 因果贡献 |
| `DecisionCounterfactualArtifact` | 在版本化 admissible-intervention policy 与依赖图下重放 score/rank/TopN/decision | “现实中改变该特征必然造成该结果” |
| `OutcomeAssociationArtifact` | explanation 与 resolution/execution rollup 的带不确定性条件关联 | 未满足识别假设时的 causal attribution |
| `ExecutionTrajectoryArtifact` | 每个 `order_intent_id` 的 PIT MAE/MFE path | canonical execution truth |
| `PolicyCounterfactualOutcome` | 明确 alternative exit/barrier policy 下的 missed return | realized return |

所有 artifact 都是 content-addressed、immutable、PIT-bound。cycle N 的 recipe planner 只能消费
`available_at <= cutoff` 且来自先前 cycle/training cohort 的 artifact；N 的 evaluation/shadow
产物最早供 N+1 使用。

weighted 与 GBDT production materializer 均已接通。GBDT training 会从实际 smartcore estimator
提取 content-addressed portable tree preimage、training-background cover 与 reference prediction
verification；core 只从已验证的 production model-input events 按冻结列序重建输入，并重新校验
run/version/family、input contract、transform 与 training-input hashes 后运行 exact TreeSHAP。
缺少任一 preimage、completion、列或 identity commitment 都 fail closed。

## 5. Weather 8-family contract

每个 family 都有独立 typed `MarketSubject`、模板解析、单位、comparator、local/UTC window、
rounding、source precedence、PIT projection、feature 和 final-label contract。禁止 generic regex
猜测或 parser fallback。

| Family | Forecast/PIT feature | Final label authority |
|---|---|---|
| Daily Temperature | GHCNd/HKO/AviationWeather observation | 同一官方 observation contract |
| Precipitation | GHCNd/HKO observation；GEFS 仅 forecast feature | GHCNd/HKO final observation |
| AQI | AirNow/EPA official product | official daily AQI；禁止自行聚合小时值伪造 daily label |
| Tornado | SPC preliminary | NCEI Storm Events final record |
| Tropical Cyclone | NHC advisories | TCR/HURDAT2 post-analysis |
| Global Temperature | NASA GISTEMP monthly anomaly | exact dataset version + base period |
| Sea Ice | NSIDC Sea Ice Index v4 | hemisphere/product + 15% extent threshold |
| Wind Extreme | GHCNd/HKO/AviationWeather observation | exact observation/window contract |

8/8 family 必须确定性进入 `Supported`、`CredentialBlocked`、`InsufficientEvidence` 或 `Excluded`；
已识别模板不得返回 `RecognizedWeatherFamilyParserUnavailable`。

## 6. 唯一 serving authority

- 删除全局可变 publication lifecycle 作为 serving truth；`Inactive/Shadow/Champion` 只按具体
  route generation 派生。
- `ModelCandidateManifest` 一次绑定 model bytes、dataset、feature/input transform、calibration、
  CPCV path set、explanation validation 和 `PromotionGateArtifact`；封存后不可 rebind。
- `PromotionGateArtifact` 绑定 TruthFreeze、AttributionPlan、Validation、Comparison、Shadow、
  feature parity、policy/profile hashes 与最终 aggregate hash。
- bootstrap、feedback promotion 与 config rollback 都由唯一 route owner 管理；旧模型
  publish/bind mutation 和 runner best-effort Shadow mutation 已删除。

唯一新增 mutation API：

```text
POST /research/model-route-activation-permits
POST /research/model-route-activation-permits/{permit_id}/revoke
POST /research/model-route-activations
```

permit request 只接收 `feedback_cycle_id`、`ttl_secs`、`idempotency_key`、`reason_code`、`note`。
TTL 默认 30 分钟，范围 5–60 分钟，按 PostgreSQL clock。candidate、route、mode、base revision 与
gate hash 全由服务端推导。

activation request 只携带 permit、cycle、expected revisions、idempotency、reason code 和 note。
事务重新校验 stale/expired/revoked permit、aggregate hash、route/runtime revision，以及 execution/
capital authority 不变量；成功 receipt 包含 before/after generation、activation/audit/outbox IDs、
transaction hash、server timestamp 和 actor lineage。允许同一 actor 完成两步，但两步分别审计。

已删除且不得恢复：

```text
POST /research/models/{id}/publish
POST /research/models/{id}/bind-calibration
POST /research/models/{id}/bind-publish-path-set
```

rollback 继续复用 config revision rollback，不新增第二套 rollback API。

## 7. UI/UX 与运维

- `cycle_id` deep link 精确请求单个 cycle；404 独立展示，不 fallback 到列表首项。
- CandidateReady 主视图展示 champion/challenger、coverage、DSR/PBO、rank IC、calibration、
  shadow delta、attribution summary、blockers 与 route diff；raw hashes 下沉 evidence disclosure。
- permit 提供 5/15/30/60 分钟预设，展示 server clock、UTC expiry 和 countdown。activation
  二次确认明确说明 route/serving 变化以及 execution mode、capital、signing authority 不变。
- 成功后展示 typed receipt、actor lineage 与现有 Model Routing rollback 深链。
- scheduler panel 展示 next due、cooldown、lease/retry、pause/resume、backlog；truth operations
  展示 inbox age、quarantine、projection watermark 与 rollup lag。
- Domain Sources 分离 declared、observed/live、stale、blocked、unobserved，不把 declared count
  标为 active。
- healthy WS 不 polling；degraded/recovery 显示可见 banner 并启用 bounded polling，恢复时强制
  authoritative refresh。所有 load/action error 都有可见 alert 与 retry。
- 延续 design tokens、i18n、keyboard/focus、非颜色单一编码、Axe 和四视口双主题约束。

## 8. 单实例生产运维边界

1. systemd/ECS service desired count 固定为 1；不实现 leader election 或多副本 scheduler。
2. PostgreSQL lease 仍必须存在，用于进程崩溃、systemd restart、stale work recovery 和同进程任务
   CAS，不能退回 process-local cursor。
3. source inbox/outbox、feedback scheduler、research jobs、permit 和 route transaction 都在数据库
   commit 后才对运行时可见。
4. runtime apply 失败时 durable route 仍是 authority；同一实例重启后从 committed generation 收敛。
5. source inbox age、quarantine、projector lag、rollup lag、scheduler overdue、stage latency、
   attribution efficiency failure、quality-gate reason、permit expiry、promotion conflict 与 WS
   recovery 必须有 metrics/alerts。

## 9. Closure 与 Activation 分离

`Implementation Closure` 只证明软件合同和 fresh production-like stacks。它不证明当前真实数据、
credentials、资金或模型足以激活某条 route。

单 route 的 `Operational Activation` 还要求：

1. authenticated current source/outcome reads；
2. profile/family mature-label 门槛；
3. 当前数据重新生成的 TruthFreeze、AttributionPlan、Validation、Comparison、Shadow 与 gate；
4. 至少一个完整 `retraining_cooldown` 的真实 ReportOnly shadow；
5. operator 先签 permit，再执行 activation；
6. activation 后验证 receipt、route generation 与现有 config rollback。

证据不足的 Weather family 保持 typed `InsufficientEvidence`。

## 10. Current-byte 验收门禁

### 10.1 数据与 fault injection

- unknown market、source revision、cursor/transaction 每个 crash point、ClickHouse outage、mapping replay；
- 多 intents 的失败/取消/提交/成交混合，证明 rollup 不早封；
- observation/canonical fact 零丢失、零重复。

### 10.2 Scheduler、attribution 与 Weather

- 单实例 restart、DB clock、cooldown、pause/resume、lease recovery、Scheduled/Manual 同 cutoff 收敛；
- 自动链路最多到 CandidateReady；
- weighted/TreeSHAP efficiency、PIT trajectory、dependency-aware counterfactual、causal naming、
  N→N+1 leakage 与完整 trial accounting；
- 8-family official golden payload、comparator boundary、unit、rounding、timezone/DST、
  preliminary/final precedence、correction 与 unknown-template fail-closed。

### 10.3 Governance 与 UI

- 旧 publish/bind routes 404/absence；
- permit expiry/revoke/stale/replay、同 actor 双动作、route conflict、transaction rollback 与
  execution/capital invariant；
- exact deep link、WS degradation/recovery、可见 error、quarantine、
  CandidateReady→permit→activation→receipt→rollback；
- 四视口、双主题、keyboard 与 Axe。

### 10.4 工程门禁

```bash
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo xtask architecture audit-functions
cargo xtask architecture check
cargo test --workspace
pnpm --dir ui lint
pnpm --dir ui check:type
pnpm --dir ui test:unit
pnpm --dir ui build:antdv-next
```

另需 Rust↔TypeScript schema sync、两套独立 fresh production-like stacks 和 fault-injection replay。
本轮全部通过；未来任何影响这些合同的变更都必须重新执行相同门禁，不能继承本轮结论。

## 11. Implementation Ledger

| ID | 状态 | 依赖 | 任务 | 完成证据 |
|---|---|---|---|---|
| W5-01 | DONE | 无 | Resolution inbox、attempt outcome、terminal rollup、durable reconciliation 与 TruthFreeze barrier | targeted repository/core/schema tests |
| W5-02 | DONE | W5-01 | 单实例 DB scheduler、cooldown/pause/retry、完整 DAG 与唯一 quality gate | scheduler/coordinator/quality targeted tests |
| W5-03 | DONE | W5-01,W5-02 | 三类 attribution、trajectory/policy counterfactual、N→N+1 leakage 与 production explanation 闭环 | 567 个 `ml-classical` research tests + core production-input bridge tests |
| W5-04 | DONE | W5-01 | Weather 8-family typed parser/source/feature/label contract | research/API/readiness focused suites |
| W5-05 | DONE | W5-02,W5-03 | 唯一 ModelRouteGovernanceService、fresh-route bootstrap、manifest/gate/permit/activation、rollback 与旧 API absence；空库不得因缺少 champion 永久阻塞首个受治理模型 | fresh calibration/bootstrap + CandidateReady→activation real PostgreSQL contracts |
| W5-06 | DONE | W5-05 | Feedback UI/UX、scheduler/truth ops、WS recovery 与 rollback journey | 93 files / 589 unit tests、lint/typecheck/build、fresh-stack CandidateReady→receipt E2E、144-state responsive/Axe matrix |
| W5-07 | DONE | W5-03,W5-04,W5-05,W5-06 | 11.9/11.10/Phase index/runbook/model catalog 收敛为唯一 current contract | old lifecycle/dead-path absence scan + fresh PG/API schema fixed-point |
| W5-08 | DONE | W5-07 | 全 workspace、schema、fresh stacks、fault injection 与 final evidence regeneration | §10 complete current-byte gates + immutable schema/UI evidence hashes |

## 12. Evidence Ledger

| 日期 | Item | 结果 | Current evidence |
|---|---|---|---|
| 2026-07-31 | W5-01 | PASS | PostgreSQL WORM inbox/projection、attempt/rollup repositories、TruthFreeze typed barrier 与 crash-safe reconciliation 定向测试已通过；barrier/queue count 与 frontier 改为同一 repeatable-read snapshot，pending attempt 的 watermark 回归 contract 在真实 PostgreSQL 通过；完整 fault matrix 已在 W5-08 重跑 |
| 2026-07-30 | W5-02 | PASS | scheduler/coordinator 定向合约覆盖 DB clock、cooldown、pause/retry、manual/scheduled identity 与 CandidateReady stop boundary |
| 2026-07-31 | W5-03 | PASS | `quant-pivot-research --features ml-classical --lib` 567/567 PASS，覆盖 actual smartcore GBDT extraction、portable replay、exact TreeSHAP efficiency 与 leakage；core production-input bridge 覆盖 frozen ordering、identity/hash drift 与 non-finite fail-closed |
| 2026-07-30 | W5-04 | PASS | research Weather 31、API Weather 33、catalog classification 10 与 vertical readiness contract 通过，8-family selector/source contract 已闭合 |
| 2026-07-31 | W5-05 | PASS | fresh calibration 生成 immutable derived ModelVersion；真实 PostgreSQL bootstrap/promotion contracts 验证唯一 route owner、manifest/gate、permit/activation transaction 与 receipt |
| 2026-07-31 | W5-06 | PASS | UI authoritative overview 拒绝 revision/count/time/frontier 矛盾；permit 使用 server clock + monotonic elapsed，activation 启用高风险确认词；permit/scheduler reads 通过 shared coordinator 串行合并 WS invalidation。lint、Vue typecheck、93 files / 589 unit tests、production build、3/3 fresh-stack governance/WS E2E 与 144-state 双主题四视口 keyboard/touch/overflow/Axe matrix 全通过；matrix `automated_findings=[]`，aggregate SHA-256=`043e43713a40bca6e362ce5f454e16efe536d2dba74b74842c3ca0fd0ccfd308` |
| 2026-07-31 | W5-07 | PASS | 删除残留 `model_not_published`/`config_overlay` wire 与 fresh-v1 enum，修正 architecture/persistence/Phase 3/Phase 10 current docs；targeted Rust all-target check、disposable PG16 `manifest-clean`、research/config schema generate-and-diff、旧 lifecycle/dead-path absence scan及 root/UI diff-check 全通过 |
| 2026-07-31 | W5-08 | PASS | `cargo fmt --check`、workspace Clippy `-D warnings`、两项 architecture gates 与 `cargo test --workspace` 全通过；真实 PostgreSQL/ClickHouse fault contracts 覆盖 unknown market、revision、cursor/transaction crash、outage/replay、multi-intent terminal rollup、scheduler recovery 与 governance conflicts。两次独立 fresh production-stack boot 均通过 authenticated REST/WS 与 CandidateReady→permit→activation→receipt→rollback；disposable PostgreSQL 16 重建 manifest 固定点为 `dff329…f465` / `f0e3ce…7c39`，Rust↔TypeScript schema sync、UI full gates 与 diff-check 均通过 |

## 13. 决策账本

| 日期 | 决策 | 原因 | 影响 |
|---|---|---|---|
| 2026-07-30 | 撤回旧 `Implementation Closure=DONE` | 旧证据没有覆盖 inbox 零丢失、attempt rollup、quality-gate bypass、唯一 serving authority、真实 scheduler 与 8-family parser | 旧 manifests 只作历史；W5-08 必须从 current bytes 重建 |
| 2026-07-30 | AWS 单实例，不建设多副本协议 | 用户部署拓扑明确为单实例 | 保留 PostgreSQL durability/lease/CAS/transaction；删除 leader-election 与 replica-convergence 需求 |
| 2026-07-30 | 首个 route 必须有显式 genesis 治理 | fresh policy bundle 没有 active pointer，而 feedback cycle 需要 champion；若只实现 promotion，空库会形成不可达状态 | bootstrap 仍由 `ModelRouteGovernanceService` 独占，要求真实模型、PIT/CPCV/quality/explanation/parity 证据、原子 policy/audit/outbox；禁止恢复旧 publish API 或自动选择首个模型 |
| 2026-07-30 | 11.10 全量合并到 11.9 | attribution 是 recipe planning 和 promotion gate 的同一闭环，独立 phase 会形成循环依赖 | Phase 11 图只保留一个 owner；06.6 直接 superseded by 11.9 |
| 2026-07-30 | execution truth 拆为 attempt 与 terminal recommendation rollup | recommendation 可拥有多个 intent/attempt，首个 outcome 会过早封存 | feedback cohort 只读取最终 rollup |
| 2026-07-30 | serving truth 只认 route generation | 全局 mutable publication status 与 route promotion 会形成双 authority | 删除旧 publish/bind mutation；rollback 复用 config revision |
| 2026-07-30 | prediction explanation、decision counterfactual 与 outcome association 分名 | score contribution × realized PnL 不是因果 PnL attribution | API/UI/artifact 禁止 causal overclaim |

## 14. 权威参考

- [PostgreSQL locking clause](https://www.postgresql.org/docs/17/sql-select.html)
- [Transactional outbox](https://docs.aws.amazon.com/prescriptive-guidance/latest/cloud-design-patterns/transactional-outbox.html)
- [Deflated Sharpe Ratio](https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2460551)
- [Probability of Backtest Overfitting](https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2326253)
- [TreeSHAP](https://arxiv.org/abs/1802.03888)
- [Feature relevance as a causal problem](https://arxiv.org/abs/1910.13413)
- [Causal Shapley values](https://arxiv.org/abs/2011.01625)
- [AirNow daily AQI](https://www.airnow.gov/publications/air-quality-index/technical-assistance-document-for-reporting-the-daily-aqi/)
- [GHCNd](https://www.ncei.noaa.gov/products/land-based-station/global-historical-climatology-network-daily)
- [NCEI Storm Events](https://www.ncei.noaa.gov/stormevents/details.jsp)
- [NHC archive](https://www.nhc.noaa.gov/data/)
- [NASA GISTEMP](https://data.giss.nasa.gov/gistemp/)
- [NSIDC Sea Ice Index v4](https://nsidc.org/data/g02135/versions/4)
- [HKO climate data](https://www.hko.gov.hk/en/cis/climat.htm)
- [Google MLOps CT](https://docs.cloud.google.com/architecture/mlops-continuous-delivery-and-automation-pipelines-in-machine-learning)
- [Google AI/ML reliability](https://docs.cloud.google.com/architecture/framework/perspectives/ai-ml/reliability)
- [NIST AI RMF](https://airc.nist.gov/airmf-resources/airmf/5-sec-core/)
